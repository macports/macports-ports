--- a/libcxxabi/src/CMakeLists.txt
+++ b/libcxxabi/src/CMakeLists.txt
@@ -235,12 +235,15 @@ function(export_symbols file)
   endif()
 endfunction()
 
 function(reexport_symbols file)
   export_symbols("${file}")
-  # -reexported_symbols_list is only available on Apple platforms
+  # -reexported_symbols_list is only available on Apple platforms and ld64
+  # rejects it for deployment targets earlier than 10.7.
   if (APPLE)
-    target_link_libraries(cxxabi-reexports INTERFACE "-Wl,-reexported_symbols_list,${file}")
+    if (NOT CMAKE_OSX_DEPLOYMENT_TARGET OR NOT CMAKE_OSX_DEPLOYMENT_TARGET VERSION_LESS "10.7")
+      target_link_libraries(cxxabi-reexports INTERFACE "-Wl,-reexported_symbols_list,${file}")
+    endif()
   endif()
 endfunction()
 
 export_symbols("${CMAKE_CURRENT_SOURCE_DIR}/../lib/symbols-not-reexported.exp")
