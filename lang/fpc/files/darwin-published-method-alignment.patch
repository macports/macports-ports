--- compiler/ncgvmt.pas
+++ compiler/ncgvmt.pas
@@ -527,6 +527,7 @@
     procedure TVMTWriter.genpublishedmethodstable(tcb: ttai_typedconstbuilder; out lab: tasmlabel; out pubmethodsdef: trecorddef);
       var
          count : longint;
+         packrecords : longint;
          lists : tvmtasmoutput;
          pubmethodsarraydef: tarraydef;
       begin
@@ -540,7 +541,11 @@
                      addr : codepointer;
                   end;
               }
-              lists.methodnamerec:=get_recorddef(itp_vmt_intern_tmethodnamerec,[cpointerdef.getreusable(cshortstringtype),voidcodepointertype],1);
+              if (tf_requires_proper_alignment in target_info.flags) then
+                packrecords:=0
+              else
+                packrecords:=1;
+              lists.methodnamerec:=get_recorddef(itp_vmt_intern_tmethodnamerec,[cpointerdef.getreusable(cshortstringtype),voidcodepointertype],packrecords);
               { from objpas.inc:
                   tmethodnametable = packed record
                     count : dword;
@@ -548,7 +553,7 @@
                   end;
                }
               tcb.start_internal_data_builder(current_asmdata.AsmLists[al_const],sec_rodata,_class.vmt_mangledname,lists.pubmethodstcb,lab);
-              get_tabledef(itp_vmt_intern_tmethodnametable,u32inttype,lists.methodnamerec,count,1,pubmethodsdef,pubmethodsarraydef);
+              get_tabledef(itp_vmt_intern_tmethodnametable,u32inttype,lists.methodnamerec,count,packrecords,pubmethodsdef,pubmethodsarraydef);
               { begin tmethodnametable }
               lists.pubmethodstcb.maybe_begin_aggregate(pubmethodsdef);
               { emit count field }
--- rtl/inc/objpas.inc
+++ rtl/inc/objpas.inc
@@ -454,14 +454,26 @@
         end;
 
       type
-         tmethodnamerec = packed record
+         tmethodnamerec =
+{$ifndef FPC_REQUIRES_PROPER_ALIGNMENT}
+         packed
+{$endif FPC_REQUIRES_PROPER_ALIGNMENT}
+         record
             name : pshortstring;
             addr : codepointer;
          end;
 
-         tmethodnametable = packed record
+         tmethodnametable =
+{$ifndef FPC_REQUIRES_PROPER_ALIGNMENT}
+         packed
+{$endif FPC_REQUIRES_PROPER_ALIGNMENT}
+         record
            count : dword;
-           entries : packed array[0..0] of tmethodnamerec;
+           entries :
+{$ifndef FPC_REQUIRES_PROPER_ALIGNMENT}
+           packed
+{$endif FPC_REQUIRES_PROPER_ALIGNMENT}
+           array[0..0] of tmethodnamerec;
          end;
 
          pmethodnametable =  ^tmethodnametable;
