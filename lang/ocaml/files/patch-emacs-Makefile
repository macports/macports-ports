--- emacs/Makefile.orig	Tue Mar  2 07:29:15 2004
+++ emacs/Makefile	Tue Mar  2 07:30:06 2004
@@ -46,11 +46,11 @@
 	$(MAKE) NOCOMPILE=true install
 
 simple-install:
-	@echo "Installing in $(EMACSDIR)..."
-	if test -d $(EMACSDIR); then : ; else mkdir -p $(EMACSDIR); fi
-	cp $(FILES) $(EMACSDIR)
+	@echo "Installing in $(DESTROOT)$(EMACSDIR)..."
+	if test -d $(DESTROOT)$(EMACSDIR); then : ; else mkdir -p $(DESTROOT)$(EMACSDIR); fi
+	cp $(FILES) $(DESTROOT)$(EMACSDIR)
 	if [ -z "$(NOCOMPILE)" ]; then \
-	  cd $(EMACSDIR); $(EMACS) --batch --eval '$(COMPILECMD)'; \
+	  cd $(DESTROOT)$(EMACSDIR); $(EMACS) --batch --eval '$(COMPILECMD)'; \
 	fi
 
 ocamltags:	ocamltags.in
@@ -58,7 +58,7 @@
 	chmod a+x ocamltags
 
 install-ocamltags: ocamltags
-	cp ocamltags $(SCRIPTDIR)/ocamltags
+	cp ocamltags $(DESTROOT)$(SCRIPTDIR)/ocamltags
 
 clean:
 	rm -f ocamltags *~ #*#
