--- otherlibs/num/Makefile.orig	Tue Mar  2 10:29:16 2004
+++ otherlibs/num/Makefile	Tue Mar  2 10:29:36 2004
@@ -53,14 +53,14 @@
 $(CAMLOBJS:.cmo=.cmx): ../../ocamlopt
 
 install:
-	if test -f dllnums.so; then cp dllnums.so $(STUBLIBDIR)/dllnums.so; fi
-	cp libnums.a $(LIBDIR)/libnums.a
-	cd $(LIBDIR); $(RANLIB) libnums.a
-	cp nums.cma $(CMIFILES) $(CMIFILES:.cmi=.mli) $(LIBDIR)
+	if test -f dllnums.so; then cp dllnums.so $(DESTROOT)$(STUBLIBDIR)/dllnums.so; fi
+	cp libnums.a $(DESTROOT)$(LIBDIR)/libnums.a
+	cd $(DESTROOT)$(LIBDIR); $(RANLIB) libnums.a
+	cp nums.cma $(CMIFILES) $(CMIFILES:.cmi=.mli) $(DESTROOT)$(LIBDIR)
 
 installopt:
-	cp $(CAMLOBJS:.cmo=.cmx) nums.cmxa nums.a $(LIBDIR)
-	cd $(LIBDIR); $(RANLIB) nums.a
+	cp $(CAMLOBJS:.cmo=.cmx) nums.cmxa nums.a $(DESTROOT)$(LIBDIR)
+	cd $(DESTROOT)$(LIBDIR); $(RANLIB) nums.a
 
 partialclean:
 	rm -f *.cm*
