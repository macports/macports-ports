--- otherlibs/graph/Makefile.orig	Tue Mar  2 10:27:09 2004
+++ otherlibs/graph/Makefile	Tue Mar  2 10:27:29 2004
@@ -50,14 +50,14 @@
 	rm -f *.a *.so *.o
 
 install:
-	if test -f dllgraphics.so; then cp dllgraphics.so $(STUBLIBDIR)/dllgraphics.so; fi
-	cp libgraphics.a $(LIBDIR)/libgraphics.a
-	cd $(LIBDIR); $(RANLIB) libgraphics.a
-	cp graphics.cm[ia] graphicsX11.cmi graphics.mli graphicsX11.mli $(LIBDIR)
+	if test -f dllgraphics.so; then cp dllgraphics.so $(DESTROOT)$(STUBLIBDIR)/dllgraphics.so; fi
+	cp libgraphics.a $(DESTROOT)$(LIBDIR)/libgraphics.a
+	cd $(DESTROOT)$(LIBDIR); $(RANLIB) libgraphics.a
+	cp graphics.cm[ia] graphicsX11.cmi graphics.mli graphicsX11.mli $(DESTROOT)$(LIBDIR)
 
 installopt:
-	cp graphics.cmx graphics.cmxa graphics.a $(LIBDIR)
-	cd $(LIBDIR); $(RANLIB) graphics.a
+	cp graphics.cmx graphics.cmxa graphics.a $(DESTROOT)$(LIBDIR)
+	cd $(DESTROOT)$(LIBDIR); $(RANLIB) graphics.a
 
 .SUFFIXES: .ml .mli .cmo .cmi .cmx
 
