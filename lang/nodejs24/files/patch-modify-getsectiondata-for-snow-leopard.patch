--- ./deps/v8/src/base/platform/platform-darwin.cc	2025-07-14 17:30:53.000000000 -0600
+++ ./deps/v8/src/base/platform/platform-darwin.cc	2025-07-14 17:35:04.000000000 -0600
@@ -86,13 +86,14 @@
   for (unsigned int i = 0; i < images_count; ++i) {
     const mach_header* header = _dyld_get_image_header(i);
     if (header == nullptr) continue;
-    unsigned long size;
 #if V8_HOST_ARCH_I32
-    uint8_t* code_ptr = getsectiondata(header, SEG_TEXT, SECT_TEXT, &size);
+    uintptr_t size;
+    const void* code_ptr = getsectdatafromheader(header, SEG_TEXT, SECT_TEXT, &size);
 #else
+    uint64_t size;
     const mach_header_64* header64 =
         reinterpret_cast<const mach_header_64*>(header);
-    uint8_t* code_ptr = getsectiondata(header64, SEG_TEXT, SECT_TEXT, &size);
+    const void* code_ptr = getsectdatafromheader_64(header64, SEG_TEXT, SECT_TEXT, &size);
 #endif
     if (code_ptr == nullptr) continue;
     const intptr_t slide = _dyld_get_image_vmaddr_slide(i);
