Fix build with poppler >= 26.01.0:
- gfree() moved; use g_free() instead
- ImageStream::reset() renamed to rewind()
- Add missing gmem.h include
--- src/extension/internal/pdfinput/pdf-input.cpp.orig
+++ src/extension/internal/pdfinput/pdf-input.cpp
@@ -283,7 +283,7 @@
     }
 #endif
     if (_thumb_data) {
-            gfree(_thumb_data);
+            g_free(_thumb_data);
     }
 }

@@ -511,7 +511,7 @@
     // Try to get a thumbnail from the PDF if possible
     if (!_render_thumb) {
         if (_thumb_data) {
-            gfree(_thumb_data);
+            g_free(_thumb_data);
             _thumb_data = nullptr;
         }
         if (!_previewed_page->loadThumb(&_thumb_data,
--- src/extension/internal/pdfinput/svg-builder.cpp.orig
+++ src/extension/internal/pdfinput/svg-builder.cpp
@@ -28,6 +28,7 @@
 #ifdef HAVE_POPPLER

 #include <poppler/Function.h>
+#include <poppler/goo/gmem.h>
 #include <poppler/GfxFont.h>
 #include <poppler/GfxState.h>
 #include <poppler/Page.h>
@@ -2093,7 +2094,7 @@
         } else {
             image_stream = new ImageStream(str, width, 1, 1);
         }
-        image_stream->reset();
+        image_stream->rewind();

         // Convert grayscale values
         unsigned char *buffer = new unsigned char[width];
@@ -2119,7 +2120,7 @@
         image_stream = new ImageStream(str, width,
                                        color_map->getNumPixelComps(),
                                        color_map->getBits());
-        image_stream->reset();
+        image_stream->rewind();

         // Convert RGB values
         unsigned int *buffer = new unsigned int[width];
