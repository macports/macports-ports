--- Source/AssetRipper.IO.Files/LocalFileSystem.cs.orig
+++ Source/AssetRipper.IO.Files/LocalFileSystem.cs
@@ -18,6 +18,19 @@ public partial class LocalFileSystem : FileSystem
 
 	public static string ExecutingDirectory => AppContext.BaseDirectory;
 
+	public static string UserDataDirectory
+	{
+		get
+		{
+			string baseDirectory = Environment.GetFolderPath(Environment.SpecialFolder.LocalApplicationData);
+			string directory = string.IsNullOrEmpty(baseDirectory)
+				? System.IO.Path.Join(System.IO.Path.GetTempPath(), "AssetRipper")
+				: System.IO.Path.Join(baseDirectory, "AssetRipper");
+			System.IO.Directory.CreateDirectory(directory);
+			return directory;
+		}
+	}
+
 	private string LocalTemporaryDirectory => Path.Join(ExecutingDirectory, "temp", GetRandomString()[0..4]);
 
 	private string SystemTemporaryDirectory => Path.Join(System.IO.Path.GetTempPath(), "AssetRipper", GetRandomString()[0..4]);
--- Source/AssetRipper.Import/Logging/FileLoggerBase.cs.orig
+++ Source/AssetRipper.Import/Logging/FileLoggerBase.cs
@@ -7,6 +7,6 @@ public abstract class FileLoggerBase : ILogger
 	protected readonly string filePath;
 
-	public FileLoggerBase() : this(Path.Join(LocalFileSystem.ExecutingDirectory, "AssetRipper.log")) { }
+	public FileLoggerBase() : this(Path.Join(LocalFileSystem.UserDataDirectory, "AssetRipper.log")) { }
 
 	/// <param name="filePath">The absolute path to the log file</param>
 	public FileLoggerBase(string filePath)
--- Source/AssetRipper.GUI.Web/WebApplicationLauncher.cs.orig
+++ Source/AssetRipper.GUI.Web/WebApplicationLauncher.cs
@@ -78,7 +78,7 @@ public static class WebApplicationLauncher
 		{
 			if (string.IsNullOrEmpty(logPath))
 			{
-				logPath = Path.Join(LocalFileSystem.ExecutingDirectory, $"AssetRipper_{DateTime.Now:yyyyMMdd_HHmmss}.log");
+				logPath = Path.Join(LocalFileSystem.UserDataDirectory, $"AssetRipper_{DateTime.Now:yyyyMMdd_HHmmss}.log");
 				RotateLogs(logPath);
 			}
 			Logger.Add(new FileLogger(logPath));
--- Source/AssetRipper.Export/Configuration/SerializedSettings.cs.orig
+++ Source/AssetRipper.Export/Configuration/SerializedSettings.cs
@@ -9,7 +9,7 @@ public readonly record struct SerializedSettings(ImportSettings Import, Processi
 {
 	public const string DefaultFileName = "AssetRipper.Settings.json";
 
-	public static string DefaultFilePath => Path.Join(LocalFileSystem.ExecutingDirectory, DefaultFileName);
+	public static string DefaultFilePath => Path.Join(LocalFileSystem.UserDataDirectory, DefaultFileName);
 
 	public static SerializedSettings Load(string path)
 	{
--- Source/AssetRipper.Import/Configuration/CoreConfiguration.cs.orig
+++ Source/AssetRipper.Import/Configuration/CoreConfiguration.cs
@@ -56,7 +56,7 @@ public abstract class CoreConfiguration : BaseConfiguration
 
 	public virtual void ResetToDefaultValues()
 	{
-		ExportRootPath = Path.Join(LocalFileSystem.ExecutingDirectory, "Ripped");
+		ExportRootPath = Path.Join(LocalFileSystem.UserDataDirectory, "Ripped");
 		SingletonData.Clear();
 		ListData.Clear();
 	}
