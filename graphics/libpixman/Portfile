# $Id$

PortSystem              1.0

name                    libpixman
set my_name             pixman
version                 0.10.0
categories              graphics
maintainers             ryandesign
homepage                http://www.x.org/
master_sites            ${homepage}pub/individual/lib/
distname                ${my_name}-${version}
use_bzip2               yes

description \
    Pixel region Library

long_description \
    libpixman is a generic library for manipulating pixel regions. A \
    PixRegion is a set of Y-X banded rectangles that cover the desired region.

checksums \
    md5 fb23e17a8a0308e8fad6dc153753ba9a \
    sha1 9262e945281877c42d484098de664dcf1a68d857 \
    rmd160 16c356f5fa3dfa4c0196456552daca3ab6244276

post-destroot {
    set docdir ${destroot}${prefix}/share/doc/${name}-${version}
    xinstall -d ${docdir}
    eval xinstall -m 644 -W ${worksrcpath} \
        AUTHORS \
        COPYING \
        [glob ${worksrcpath}/ChangeLog*] \
        NEWS \
        README \
        TODO \
        ${docdir}
}

livecheck.check         regex
livecheck.url           [lindex ${master_sites} 0]?C=M&O=D
livecheck.regex         ${my_name}-(\[0-9.\]+)\\.tar
