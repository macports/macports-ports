--- src/nettle/configure.orig	Tue Feb 24 10:22:18 2004
+++ src/nettle/configure	Tue Feb 24 10:22:28 2004
@@ -1821,7 +1821,7 @@
 fi;
 
 if test x$with_include_path != x ; then
-  CPPFLAGS="$CPPFLAGS -I `echo $with_include_path | sed 's/:/ -I /g'`"
+  CPPFLAGS="$CPPFLAGS -I`echo $with_include_path | sed 's/:/ -I/g'`"
 fi
 
 
@@ -1834,7 +1834,7 @@
 fi;
 
 if test x$with_lib_path != x ; then
-  LDFLAGS="$LDFLAGS -L `echo $with_lib_path | sed 's/:/ -L /g'`"
+  LDFLAGS="$LDFLAGS -L`echo $with_lib_path | sed 's/:/ -L/g'`"
 fi
 
 # Check whether --enable-public-key or --disable-public-key was given.
