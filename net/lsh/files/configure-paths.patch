Do not look for files in all these paths.
--- configure.orig	2013-06-26 06:01:23.000000000 -0500
+++ configure	2018-11-01 18:08:57.000000000 -0500
@@ -4787,8 +4787,7 @@
 
 for d in `echo $with_lib_path | sed 's/:/ /g'` \
     `echo $exec_prefix | sed "s@^NONE@$prefix/lib@g" | sed "s@^NONE@$ac_default_prefix/lib@g"` \
-    /usr/local/lib /sw/local/lib /sw/lib \
-    /usr/gnu/lib /opt/gnu/lib /sw/gnu/lib /usr/freeware/lib /usr/pkg/lib ; do
+    ; do
   { $as_echo "$as_me:${as_lineno-$LINENO}: checking $d" >&5
 $as_echo_n "checking $d... " >&6; }
 ac_exists=no
--- src/spki/configure.orig	2013-06-15 09:05:11.000000000 -0500
+++ src/spki/configure	2018-11-01 18:08:58.000000000 -0500
@@ -2915,8 +2915,7 @@
 
 for d in `echo $with_lib_path | sed 's/:/ /g'` \
     `echo $exec_prefix | sed "s@^NONE@$prefix/lib@g" | sed "s@^NONE@$ac_default_prefix/lib@g"` \
-    /usr/local/lib /sw/local/lib /sw/lib \
-    /usr/gnu/lib /opt/gnu/lib /sw/gnu/lib /usr/freeware/lib /usr/pkg/lib ; do
+    ; do
   { $as_echo "$as_me:${as_lineno-$LINENO}: checking $d" >&5
 $as_echo_n "checking $d... " >&6; }
 ac_exists=no
