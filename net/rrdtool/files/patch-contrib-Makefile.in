--- contrib/Makefile.in.orig	Fri Feb 28 17:47:48 2003
+++ contrib/Makefile.in	Thu Mar  6 18:38:48 2003
@@ -87,10 +87,14 @@
 ZLIB_CFLAGS = @ZLIB_CFLAGS@
 ZLIB_LIB_DIR = @ZLIB_LIB_DIR@
 
-SUBDIRS = trytime
-contribdir = $(prefix)/contrib
+SUBDIRS = 
+## XYZZY: hack for now, hard code our name and version into the 
+## contribdir path.  It'd be better to pick name and version up
+## from automake/configure. 
+## contribdir = $(prefix)/contrib
+contribdir = $(prefix)/share/rrdtool-${VERSION}/contrib
 contrib_DATA = README
-EXTRA_DIST = rrdview log2rrd rrd-file-icon rrdproc rrdlastds add_ds killspike rrdfetchnames snmpstats rrdexplorer php3 php4
+EXTRA_DIST = trytime rrdview log2rrd rrd-file-icon rrdproc rrdlastds add_ds killspike rrdfetchnames snmpstats rrdexplorer php3 php4
 mkinstalldirs = $(SHELL) $(top_srcdir)/config/mkinstalldirs
 CONFIG_HEADER = ../config/config.h
 CONFIG_CLEAN_FILES = 
