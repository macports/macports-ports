--- examples/Makefile.in.orig	Fri Feb 28 17:58:12 2003
+++ examples/Makefile.in	Mon Mar  3 17:39:32 2003
@@ -93,7 +93,11 @@
 
 EXTRA_DIST = cgi-demo.cgi.in    piped-demo.pl.in   shared-demo.pl.in stripes.pl.in bigtops.pl.in minmax.pl.in
 
-examplesdir = $(prefix)/examples
+## XYZZY: hack for now, hard code our name and version into the
+## examplesdir path.  It'd be better to pick name and version up
+## from automake/configure.
+## examplesdir = $(prefix)/examples
+examplesdir = $(prefix)/share/examples/rrdtool-${VERSION}
 examples_SCRIPTS = cgi-demo.cgi piped-demo.pl shared-demo.pl stripes.pl bigtops.pl minmax.pl
 mkinstalldirs = $(SHELL) $(top_srcdir)/config/mkinstalldirs
 CONFIG_HEADER = ../config/config.h
