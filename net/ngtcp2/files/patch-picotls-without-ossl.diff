--- configure.orig	2025-09-17 22:11:39.000000000 +0200
+++ configure	2025-09-17 22:13:24.000000000 +0200
@@ -21560,7 +21560,7 @@
 have_quictls=no
 have_libressl=no
 have_ossl=no
-if test "x${request_openssl}" != "xno"; then
+if test "x${request_openssl}" != "xno" || test "x${request_picotls}" = "xyes"; then
 
 pkg_failed=no
 { printf "%s\n" "$as_me:${as_lineno-$LINENO}: checking for openssl >= 1.1.1" >&5
@@ -21641,7 +21641,10 @@
     VANILLA_OPENSSL_CFLAGS="$OPENSSL_CFLAGS"
 
 
+  fi
+fi
 
+if test "x${request_openssl}" != "xno" && test "x${have_openssl}" = "xyes"; then
 
     # Until OpenSSL gains mainline support for QUIC, check for a
     # patched version.
@@ -21770,7 +21773,6 @@
       CFLAGS="$save_CFLAGS"
       LIBS="$save_LIBS"
     fi
-  fi
 fi
 
 if test "x${request_openssl}" = "xyes" &&
