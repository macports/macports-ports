--- DarwinPortsStartup	Wed Oct  2 09:06:57 2002
+++ DarwinPortsStartup.new	Sun Jan 23 20:04:44 2005
@@ -17,7 +17,9 @@
 	for prefix in ${prefixes}; do
 		rcdir="${prefix}/etc/rc.d/"
 		for file in `ls ${rcdir}/*.sh`; do
-			/bin/sh "${file}" $1
+			if [ -x ${file} ]; then
+				/bin/sh "${file}" $1
+			fi
 		done
 	done
 }
