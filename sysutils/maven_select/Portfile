# $Id$

PortSystem 1.0
PortGroup select 1.0

name                    maven_select
version                 0.3
categories              sysutils
platforms               darwin
license                 BSD
maintainers             blair openmaintainer
supported_archs         noarch
description             Switch the default Maven java build tool
long_description        maven_select lets you switch the default Maven \
                        Java build tool and symlinks the standard Maven \
                        executables in the MacPorts prefix to the selected \
                        version.

homepage                http://svn.macports.org/repository/macports/contrib/select/
master_sites            ${homepage}
distname                select-${version}
checksums               md5     b24cecd12fc5567e544b25c5df34a255 \
                        sha1    4aa312d7e0b9ede1ef015fcd941ff53a0329127a \
                        rmd160  f121d366cdecc7d5331040abfd153334399d80d5

configure.args          --mandir=${prefix}/share/man --name=maven

# install all files
post-destroot {
        select::install maven ${filespath}/base
        select::install maven ${filespath}/none
}

platform darwin {
}

livecheck.type  none

# Documentation for maven ports:
#   PortGroup select 1.0
#   select.group            maven
#   select.file             ${filespath}/maven[string map {. {}} ${branch}]
