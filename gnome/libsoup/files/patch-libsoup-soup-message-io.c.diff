--- libsoup/soup-message-io.c.orig	2013-10-18 15:07:25.000000000 -0700
+++ libsoup/soup-message-io.c	2013-10-18 15:10:18.000000000 -0700
@@ -988,7 +988,7 @@
 	g_object_ref (msg);
 
 	if (io) {
-		g_return_if_fail (io->mode == SOUP_MESSAGE_IO_CLIENT);
+		g_return_val_if_fail (io->mode == SOUP_MESSAGE_IO_CLIENT, FALSE);
 
 		if (io->read_state < SOUP_MESSAGE_IO_STATE_BODY_DONE)
 			io->read_state = SOUP_MESSAGE_IO_STATE_BODY_DONE;
