# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:filetype=tcl:et:sw=4:ts=4:sts=4

PortSystem          1.0
PortGroup           meson 1.0

name                gnome-weather
version             41.0
revision            0
license             GPL-2+
set branch          [lrange [split ${version} .] 0 0]
description         A small application that allows you to monitor the current weather \
                    conditions for your city, or anywhere in the world
long_description    ${description}
maintainers         {devans @dbevans} openmaintainer
categories          gnome
platforms           darwin
homepage            https://wiki.gnome.org/Apps/Weather
master_sites        gnome:sources/${name}/${branch}/

use_xz              yes

checksums           rmd160  479b56a5fa0d508dc9a4337b31691ca981160fcf \
                    sha256  e5614eb74297e072960d6447557fa9fbfccf24bfcb314944b6d976640ee62dee \
                    size    943500

depends_build       port:pkgconfig

depends_lib         port:desktop-file-utils \
                    port:geoclue2 \
                    port:gjs \
                    port:gobject-introspection \
                    port:gtk3 \
                    port:libgweather \
                    port:libhandy

depends_run         port:adwaita-icon-theme \
                    port:yelp

post-activate {
   system "${prefix}/bin/gtk-update-icon-cache-3.0 -f -t ${prefix}/share/icons/hicolor"
   system "${prefix}/bin/update-desktop-database ${prefix}/share/applications"
   system "${prefix}/bin/glib-compile-schemas ${prefix}/share/glib-2.0/schemas"
}

livecheck.type      gnome
