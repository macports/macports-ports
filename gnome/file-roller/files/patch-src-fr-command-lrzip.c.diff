--- src/fr-command-lrzip.c.orig	2013-05-13 08:35:39.000000000 -0700
+++ src/fr-command-lrzip.c	2013-05-13 08:38:45.000000000 -0700
@@ -52,7 +52,11 @@
 
 	struct stat st;
 	if (stat (comm->filename, &st) == 0)
-		fdata->modified = st.st_mtim.tv_sec;
+#ifdef __APPLE__
+                fdata->modified = st.st_mtimespec.tv_sec;
+#else
+                fdata->modified = st.st_mtim.tv_sec;
+#endif
 	else
 		time(&(fdata->modified));
 	fdata->encrypted = FALSE;
