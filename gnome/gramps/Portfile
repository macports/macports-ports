# $Id $

PortSystem 1.0

name				gramps
version				2.0.10
categories			gnome science python
platforms			darwin
maintainers			rjt-gramps@thegrindstone.me.uk

description			GRAMPS is a genealogy program to store, edit, \
				and research genealogical data.  It provides \
				advanced capabilities for research, analysis, and \
				correlation to potentially fill relationship gaps.

long_description		${description}

homepage			http://www.gramps-project.org/
master_sites			sourceforge:gramps
checksums			md5 2c9c00acce995546af325087622235e5
patchfiles			patch-configure 

configure.args			--disable-mime-install

depends_lib			bin:update-mime-database:shared-mime-info \
				port:py-gnome \
				port:py-bsddb

#post-patch {
#        reinplace "s|scrollkeeper_localstate_dir = \$(localstatedir)/scrollkeeper|scrollkeeper_localstate_dir = ${prefix}/var/scrollkeeper|g" \
#                "${worksrcpath}/doc/gramps-manual/C/Makefile.in"
#	reinplace "s|scrollkeeper-preinstall \$(docdir)|scrollkeeper-preinstall \$(destroot)\$(docdir)|g" \
#		"${worksrcpath}/doc/gramps-manual/C/Makefile.in"
#}

post-activate {
	system "update-mime-database ${prefix}/share/mime"
}	

