#==================================================================================================
# From 22db29e9069042b4e5c07ee496c62c8c63e9b7d5 Mon Sep 17 00:00:00 2001
# From: Daeho Ro
# Date: Fri, 3 Jul 2026 16:27:06 +0900
# Subject: [PATCH] result-dialog: use \x1b hex escape instead of invalid \u001b
#
# The \u001b Unicode escape in the ANSI parser is emitted verbatim into the
# generated C, where newer compilers reject it as an invalid universal
# character name (values below 0xA0 are not permitted). Use the \x1b hex
# escape, as already done elsewhere in this file, which compiles cleanly
# and denotes the same ESC character.
#
# Closes #521
#==================================================================================================
--- gitg/gitg-result-dialog.vala
+++ gitg/gitg-result-dialog.vala
@@ -90,7 +90,7 @@ public class AnsiRenderer {
 		int fg = -1;
 
 		while (p < len) {
-			int escpos = s.index_of ("\u001b", p);
+			int escpos = s.index_of ("\x1b", p);
 			if (escpos < 0) {
 				// no more escapes — insert remainder
 				string seg = s.substring (p, len - p);
@@ -147,7 +147,7 @@ public class AnsiRenderer {
 			} else {
 				print("unsupported ");
 				// not a supported escape sequence, insert ESC as literal
-				insert_with_tag ("\u001b", bold, underline, fg);
+				insert_with_tag ("\x1b", bold, underline, fg);
 				p = escpos + 1;
 			}
 		}

