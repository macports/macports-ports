--- Makefile.in	Tue Aug 17 19:12:40 1993
+++ Makefile.in.new	Fri Dec 10 13:56:40 2004
@@ -95,12 +95,12 @@
 # scriptdir is the directory in which shell scripts should be installed
 datadir = $(prefix)/lib
 libdir = $(prefix)/lib
-infodir = $(prefix)/info
+infodir = $(prefix)/share/info
 
 # Extension (not including `.') for the installed manual page filenames.
 manext = 1
 # Where to install the manual pages.
-mandir = $(prefix)/man/man$(manext)
+mandir = $(prefix)/share/man/man$(manext)
 # Use manlinks=so to use the .so method instead of hard links
 manlinks = ln
 
