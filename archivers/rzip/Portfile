# $Id: Portfile,v 1.1 2005/10/28 17:11:34 mww Exp $

PortSystem 1.0

name			rzip
version			2.0
categories		archivers
platforms		darwin
maintainers		mww@opendarwin.org
description		Compression program similar to gzip or bzip2
long_description	rzip is a compression program, similar in functionality to \
			gzip or bzip2, but able to take advantage from long distance \
			redundancies in files, which can sometimes allow rzip to produce \
			much better compression ratios than other programs.

homepage		http://rzip.samba.org/
master_sites	http://rzip.samba.org/ftp/rzip/
checksums		md5 8a88b445afba919b122a3899d6d26b2a
patchfiles		patch-Makefile.in

post-extract {
	file copy ${filespath}/strndup.c ${worksrcpath}
}

configure.args	--mandir=\\\${prefix}/share/man

destroot.destdir	prefix=${destroot}${prefix}

