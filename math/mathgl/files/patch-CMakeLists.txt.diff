--- CMakeLists.txt.orig	2016-05-16 07:57:52.000000000 -0500
+++ CMakeLists.txt	2016-05-17 06:31:47.000000000 -0500
@@ -619,9 +619,9 @@
 	endif(enable-python OR enable-lua OR enable-octave)
 add_subdirectory( utils )
 add_subdirectory( examples )
-	if(NOT WIN32)
-		add_subdirectory( fonts )
-	endif(NOT WIN32)
+#	if(NOT WIN32)
+#		add_subdirectory( fonts )
+#	endif(NOT WIN32)
 # 	add_subdirectory( mgllab )
 endif(NOT enable-lgpl)
 
