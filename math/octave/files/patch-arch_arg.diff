--- src/mkoctfile.in.cc~	2018-04-30 13:03:56.000000000 -0400
+++ src/mkoctfile.in.cc	2018-05-15 02:17:32.000000000 -0400
@@ -634,6 +634,19 @@
         {
           pass_on_options += (' ' + arg);
         }
+#if defined __APPLE__ && defined __MACH__
+      else if (arg == "-arch")
+        {
+          if (i < argc - 1)
+            {
+              arg = argv[++i];
+              pass_on_options += (" -arch " + arg);
+            }
+          else
+            std::cerr << "mkoctfile: arch type missing"
+                      << std::endl;
+        }
+#endif
       else if (starts_with (arg, "-"))
         {
           // Pass through any unrecognized options.
