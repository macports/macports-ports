--- doc/minetest.6.orig	2023-01-22 13:00:10.000000000 +0100
+++ doc/minetest.6	2023-01-22 23:22:04.000000000 +0100
@@ -22,6 +22,40 @@
 The main design philosophy is to keep it technically simple, stable and
 portable. It will be kept lightweight enough to run on fairly old hardware.
 
+.SH EXAMPLES
+.B This is a Minetest (MT) port provided by MacPorts for the MacOS X / macOS platform.
+.PP
+* This particular
+.B #SUBPORT#
+subport #DESCR_CLIENT_SERVER#
+.PP
+* #DESCR_TRAILER#
+.PP
+More information at <https://ports.macports.org/port/minetest/>.
+.PP
+.B Usually, the MT App would be called via the GUI of the MacOS X / macOS platform.
+.PP
+However, one may call the program from the Terminal over the command line (CLI) as
+follows:
+.TP
+.B #PATH_CLIENT# [OPTIONS]
+Start the MT Client program (in example with full GUI support when called without any
+options) from the command line, optionally with further options (see below).
+.TP
+.B #PATH_SERVER# [OPTIONS]
+Start the MT Server program from the command line, optionally with further options (see
+below).
+.TP
+.B ls #DOCPATH#
+List the available supplementary documentary for further information as provided by the
+Minetest developers.
+.TP
+.B man #MANPAGE#
+List this particular version of the minetest manual page if applicable.
+.PP
+NOTE: The above section is an amendment by the MacPorts project and was not part of the
+original text.
+
 .SH COMMON OPTIONS
 .TP
 .B \-\-help
