--- contrib/MacOSX/Makefile.in
+++ contrib/MacOSX/Makefile.in
@@ -579,7 +579,6 @@ uninstall-am:
 	    <$< >$@
 
 install-data-local:
-	[ `uname -s` != "Darwin" ] || make install-sys-darwin
 
 install-sys-darwin:
 	@if [ `id -u` -eq 0 ]; then \
