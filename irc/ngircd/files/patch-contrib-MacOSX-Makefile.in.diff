--- contrib/MacOSX/Makefile.in
+++ contrib/MacOSX/Makefile.in
@@ -615,7 +615,6 @@ uninstall-am:
 	    <$< >$@
 
 install-data-local:
-	[ `uname -s` != "Darwin" ] || make install-sys-darwin
 
 install-sys-darwin:
 	@if [ `id -u` -eq 0 ]; then \
