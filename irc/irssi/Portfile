PortSystem 1.0
name		irssi
version		0.8.6
revision	1
categories	irc
maintainers	jpm@opendarwin.org
description	modular IRC client
long_description	Irssi is a modular IRC client that currently has only text mode user interface, but 80-90% of the code isn't text mode specific, so other UIs could be created pretty easily. Also, Irssi isn't really even IRC specific anymore, there's already a working SILC module available. Support for other protocols like ICQ could be created some day too.
platforms	darwin
master_sites	http://irssi.org/files/
checksums	md5 fea9ebf3ceec137503752e0d05538220
depends_lib 	lib:glib:glib lib:libdl:dlcompat
configure.env	CPPFLAGS=-no-cpp-precomp
configure.args	--without-socks	--without-bot --without-proxy \
	 --with-modules --disable-ipv6 --disable-ssl \
	 --with-glib-prefix=${prefix}
include		contents
variant ipv6 {
	configure.args-append --enable-ipv6
}
variant ssl {
#	depends_lib-append	lib:libssl:libssl
	configure.args-append --enable-ssl
}
