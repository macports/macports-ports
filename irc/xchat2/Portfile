PortSystem 1.0
name		xchat2
version		1.9.8
categories	irc
maintainers	mij@opendarwin.org
description	IRC client for gtk2
platforms	darwin

long_description XChat2 is a graphical IRC client based on GTK2 that has \
		 many features and can be scripted with Perl and/or Python.

master_sites	http://www.xchat.org/files/source/1.9/

distname	xchat-${portversion}
checksums	md5 f7df856c9fec9d9776738129dec6c063

use_bzip2	yes

depends_lib	lib:libintl.1:gettext lib:libgtk.2:gtk2

patchfiles	patch-configure

configure.env	LDFLAGS="-L${prefix}/lib -L/usr/X11R6/lib" \
		CFLAGS="-I${prefix}/include" \
		CPPFLAGS="-I${prefix}/include -no-cpp-precomp" \
		LIBS="-lintl -liconv -lX11 -lXft"

variant xft	{ configure.args --enable-xft }

include		contents
