--- configure.orig	Fri Sep 26 04:06:24 2003
+++ configure	Sat Sep 27 02:36:04 2003
@@ -8786,6 +8786,8 @@
 		fi
 		if test -f $withval/lib/libexpat.a; then
         	        LIBS="$LIBS -L$withval/lib -lexpat"
+		elif test -f "$withval/lib/libexpat.0.dylib"; then
+        	        LIBS="$LIBS -L$withval/lib -lexpat"
 		else
 			echo "$as_me:$LINENO: result: no" >&5
 echo "${ECHO_T}no" >&6
