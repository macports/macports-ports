--- Source/WTF/wtf/PlatformHave.h.orig
+++ Source/WTF/wtf/PlatformHave.h
@@ -66,6 +66,10 @@
 #define HAVE_ARM_IDIV_INSTRUCTIONS 1
 #endif
 
+/* Older macOS SDKs use mach_vm_offset_t in MACH_VM_MAX_ADDRESS without
+   including its declaration; pull it in explicitly. */
+#include <mach/vm_types.h>
+
 #if CPU(ADDRESS64)
 #if HAVE(36BIT_ADDRESS)
 #define WTF_OS_CONSTANT_EFFECTIVE_ADDRESS_WIDTH 36
