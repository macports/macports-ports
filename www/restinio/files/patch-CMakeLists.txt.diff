--- dev/CMakeLists.txt.orig	2018-04-30 13:19:56.000000000 -0300
+++ dev/CMakeLists.txt	2018-04-30 13:21:29.000000000 -0300
@@ -86,13 +86,6 @@
 # HTTP parser
 add_subdirectory(nodejs/http_parser)
 
-# ------------------------------------------------------------------------------
-# SObjectizer
-SET(SOBJECTIZER_BUILD_STATIC ON)
-SET(SOBJECTIZER_LIBS sobjectizer::StaticLib)
-add_subdirectory(so_5)
-
-# ------------------------------------------------------------------------------
 # Zlib
 find_package(ZLIB)
 
@@ -104,21 +97,4 @@
 ENDIF ()
 
 message( STATUS "ZLIB_LIBRARIES='" ${ZLIB_LIBRARIES} "'" )
-message( STATUS "ZLIB_INCLUDE_DIRS='" ${ZLIB_INCLUDE_DIRS} "'" )
-
-# ------------------------------------------------------------------------------
-# Tests
-enable_testing()
-add_subdirectory(test)
-
-IF (WIN32)
-	configure_file(${CMAKE_SOURCE_DIR}/cmake/run_tests.bat ${CMAKE_BINARY_DIR} NEWLINE_STYLE WIN32)
-ENDIF ()
-
-# ------------------------------------------------------------------------------
-# Samples
-add_subdirectory(sample)
-
-# ------------------------------------------------------------------------------
-# Benches
-add_subdirectory(benches)
+message( STATUS "ZLIB_INCLUDE_DIRS='" ${ZLIB_INCLUDE_DIRS} "'" )
\ No newline at end of file
