--- Tools/MiniBrowser/gtk/main.c.orig
+++ Tools/MiniBrowser/gtk/main.c
@@ -288,11 +288,11 @@
         switch (item[0]) {
         case '!':
         case '-':
             enabled = FALSE;
-            [[fallthrough]];
+            __attribute__((fallthrough));
         case '+':
             item++;
-            [[fallthrough]];
+            __attribute__((fallthrough));
         default:
             break;
         }
