--- a/Sources/AFMKitApple/FoundationModelsRequestAdapter.swift
+++ b/Sources/AFMKitApple/FoundationModelsRequestAdapter.swift
@@ -247,7 +247,7 @@
 
     public static func foundationMetadata(
         _ values: [String: AFMJSONValue]
-    ) -> [String: any Sendable & Codable & Equatable] {
+    ) -> [String: any ConvertibleToGeneratedContent] {
         values.reduce(into: [:]) { result, item in
             switch item.value {
             case .null: result[item.key] = "null"
@@ -290,8 +290,6 @@
                     )
                 }
                 return result
-            case .custom(let custom):
-                return [.text(customContent(custom))]
             @unknown default:
                 throw LanguageModelError.unsupportedTranscriptContent(
                     .init(
@@ -302,14 +300,6 @@
             }
         }
         return parts.isEmpty ? nil : parts
-    }
-
-    private static func customContent<C: Transcript.CustomSegment>(_ custom: C) -> String {
-        if let data = try? JSONEncoder().encode(custom.content),
-           let value = String(data: data, encoding: .utf8) {
-            return value
-        }
-        return custom.description
     }
 
     private static func pngData(_ image: Transcript.ImageAttachment) throws -> Data {
@@ -372,11 +362,19 @@
     static func mergeRequestMetadata(
         into metadata: inout [String: AFMJSONValue],
         accessorAvailable: Bool,
-        read: () -> [String: any Sendable & Codable & Equatable]
+        read: () -> [String: GeneratedContent]
     ) {
         guard accessorAvailable else { return }
-        for (key, value) in afmMetadata(read()) {
-            metadata[key] = value
+        for (key, value) in read() {
+            let encoded = Data(value.jsonString.utf8)
+            if let object = try? JSONSerialization.jsonObject(
+                with: encoded,
+                options: .fragmentsAllowed
+            ), let converted = try? afmJSONValue(object) {
+                metadata[key] = converted
+            } else {
+                metadata[key] = .string(value.jsonString)
+            }
         }
     }
 
--- a/Sources/AFMKitApple/FoundationNativeProviderProbe.swift
+++ b/Sources/AFMKitApple/FoundationNativeProviderProbe.swift
@@ -106,7 +106,7 @@
         hasEntitlement: Bool,
         entitlement: String = AFMFoundationNativeProviderCapabilities.privateCloudComputeEntitlement,
         locale: Locale = .current
-    ) -> AFMFoundationPrivateCloudComputeSnapshot {
+    ) async -> AFMFoundationPrivateCloudComputeSnapshot {
         guard hasEntitlement else {
             return AFMFoundationPrivateCloudComputeSnapshot(
                 hasEntitlement: false,
@@ -143,7 +143,7 @@
             entitlement: entitlement,
             availability: availability,
             localeIdentifier: locale.identifier,
-            localeSupported: model.supportsLocale(locale),
+            localeSupported: (try? await model.supportsLocale(locale)) ?? false,
             quotaStatus: "\(quota.status)",
             quotaIsLimitReached: quota.isLimitReached,
             quotaLimitDetail: quotaLimitDetail
--- a/Sources/AFMKitApple/FoundationTranscriptSnapshotParser.swift
+++ b/Sources/AFMKitApple/FoundationTranscriptSnapshotParser.swift
@@ -146,8 +146,6 @@
                 return structure.content.jsonString
             case .attachment(let attachment):
                 return attachment.label.map { "[Attachment: \($0)]" } ?? "[Attachment]"
-            case .custom:
-                return "[Custom tool output]"
             @unknown default:
                 return "[Unknown tool output]"
             }
--- a/Sources/AFMKitApple/FoundationProvider.swift
+++ b/Sources/AFMKitApple/FoundationProvider.swift
@@ -174,7 +174,7 @@
             }
             return Self.availability(snapshot.availability)
         case .privateCloudCompute:
-            let snapshot = probe.privateCloudComputeSnapshot(
+            let snapshot = await probe.privateCloudComputeSnapshot(
                 hasEntitlement: hasPrivateCloudComputeEntitlement()
             )
             let availability = Self.availability(snapshot.availability)
