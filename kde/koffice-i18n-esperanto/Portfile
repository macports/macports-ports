# $Id$

PortSystem 1.0
name		koffice-i18n-esperanto
version		1.5.2
categories	kde
maintainers	nomaintainer@macports.org
description	KOffice esperanto language files.
long_description ${description}
platforms	darwin
homepage	http://www.koffice.org/
master_sites	kde:stable/koffice-${version}/src/koffice-l10n/:koffice160
#		opendarwin::koffice160
distname	koffice-l10n-eo-${version}
distfiles	${distname}.tar.bz2:koffice160
use_bzip2	yes
checksums	md5 c7025f7d946342267943fd146a5cc19c
depends_lib	port:koffice

patch		{
		reinplace "s|doc/HTML|doc/kde|g" ${worksrcpath}/configure
}

configure.env	HOME=${workpath} KDEDIR=${prefix}
configure.args	--without-arts

build.env	${configure.env}

post-destroot	{
		xinstall -d -m 755 ${destroot}${prefix}/share/doc/kde-installed-packages
		system "touch ${destroot}${prefix}/share/doc/kde-installed-packages/koffice-i18n-esperanto"
}

livecheck.check	none
