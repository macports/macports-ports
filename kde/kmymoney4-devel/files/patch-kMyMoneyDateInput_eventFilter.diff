--- kmymoney/kmymoney/widgets/kmymoneydateinput.cpp.orig	2010-11-14 20:04:21.000000000 +0100
+++ kmymoney/kmymoney/widgets/kmymoneydateinput.cpp	2010-11-14 20:05:54.000000000 +0100
@@ -302,10 +302,10 @@
 bool kMyMoneyDateInput::eventFilter(QObject *, QEvent *e)
 {
   if (e->type() == QEvent::FocusIn) {
-    d->m_datePopup->show(mapToGlobal(QPoint(0, height())));
+;//    d->m_datePopup->show(mapToGlobal(QPoint(0, height())));
     // select the date section, but we need to delay it a bit
   } else if (e->type() == QEvent::FocusOut)
-    d->m_datePopup->hide();
+;//    d->m_datePopup->hide();
   else if (e->type() == QEvent::KeyPress) {
     if (QKeyEvent *k = dynamic_cast<QKeyEvent*>(e)) {
       keyPressEvent(k);
