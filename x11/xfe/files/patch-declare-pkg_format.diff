--- src/main.cpp.orig	2025-09-25 09:58:05
+++ src/main.cpp	2025-09-25 10:02:17
@@ -388,7 +388,7 @@
     textdomain(PACKAGE);
 #endif
 
-#if defined(__FreeBSD__)
+#if defined(__FreeBSD__) || defined(__APPLE__)
     FXuint pkg_format;
     pkg_format = OTHER_PKG;
 #endif
--- src/xfedefs.h.orig	2025-09-25 09:57:54
+++ src/xfedefs.h	2025-09-25 10:02:43
@@ -393,7 +393,7 @@
 // Macro to add parentheses before and after a given string
 #define PARS(s)    ((FXString)" (" + (s) + (FXString)")")
 
-#if defined(__FreeBSD__)
+#if defined(__FreeBSD__) || defined(__APPLE__)
 #define OTHER_PKG    2
 #endif
 
