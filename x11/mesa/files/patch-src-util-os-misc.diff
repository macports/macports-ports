--- src/util/os_misc.c
+++ src/util/os_misc.c	2026-08-19 23:09:25.000000000 +0800
@@ -521,7 +521,7 @@
 #endif
 }
 
-#if DETECT_OS_APPLE
+#if DETECT_OS_APPLE && defined(MAP_JIT)
 
 static bool jit_allowed;
 
@@ -548,12 +548,12 @@
    jit_allowed = true;
 }
 
-#endif /* DETECT_OS_APPLE */
+#endif /* DETECT_OS_APPLE && defined(MAP_JIT) */
 
 bool
 os_jit_allowed(void)
 {
-#if DETECT_OS_APPLE
+#if DETECT_OS_APPLE && defined(MAP_JIT)
    static util_once_flag once = UTIL_ONCE_FLAG_INIT;
 
    util_call_once(&once, os_probe_jit_allowed);
