diff -uNr kdeartwork-3.4.0/kscreensaver/kdesavers/rotation.cpp kdeartwork-3.4.0-new/kscreensaver/kdesavers/rotation.cpp
--- kdeartwork-3.4.0/kscreensaver/kdesavers/rotation.cpp	Fri Jan  7 06:25:54 2005
+++ kdeartwork-3.4.0-new/kscreensaver/kdesavers/rotation.cpp	Mon Feb 28 11:08:01 2005
@@ -83,13 +83,13 @@
 EulerOdeSolver::EulerOdeSolver(
    const double &_t,
    const double &_dt,
-   const double &_A,
-   const double &_B,
-   const double &_C,
-   std::valarray<double> &_y,
+   const double &fooA,
+   const double &fooB,
+   const double &fooC,
+   std::valarray<double> &fooy,
    const double &_eps)
-   : RkOdeSolver<double>(_t,_y,_dt,_eps),
-     A(_A), B(_B), C(_C)
+   : RkOdeSolver<double>(_t,fooy,_dt,_eps),
+     A(fooA), B(fooB), C(fooC)
 {
 }
 
diff -uNr kdeartwork-3.4.0/kscreensaver/kdesavers/rotation.h kdeartwork-3.4.0-new/kscreensaver/kdesavers/rotation.h
--- kdeartwork-3.4.0/kscreensaver/kdesavers/rotation.h	Fri Apr 16 19:42:39 2004
+++ kdeartwork-3.4.0-new/kscreensaver/kdesavers/rotation.h	Mon Feb 28 11:08:01 2005
@@ -40,10 +40,10 @@
     * @param t Time in seconds, integration variable
     * @param dt Initial time increment in seconds for integration, auto adjusted
     * later to guarantee precision
-    * @param _A Moment of inertia along 1. figure axis
-    * @param _B Moment of inertia along 2. figure axis
-    * @param _C Moment of inertia along 3. figure axis
-    * @param _y Vector of 12 elements containing the initial rotation vector
+    * @param fooA Moment of inertia along 1. figure axis
+    * @param fooB Moment of inertia along 2. figure axis
+    * @param fooC Moment of inertia along 3. figure axis
+    * @param fooy Vector of 12 elements containing the initial rotation vector
     * omega (elements 0 to 2), and the initial rotating systems coordinate
     * vectors e1, e2, e3 (elements 3 to 5, 6 to 8, and 9 to 11).
     * @param eps Relative precision per integration step, see
@@ -51,10 +51,10 @@
    EulerOdeSolver(
       const double &t,
       const double &dt,
-      const double &_A,
-      const double &_B,
-      const double &_C,
-      std::valarray<double> &_y,
+      const double &fooA,
+      const double &fooB,
+      const double &fooC,
+      std::valarray<double> &fooy,
       const double &eps);
 
   protected:
