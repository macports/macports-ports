--- Makefile.PL.orig
+++ Makefile.PL
@@ -3,6 +3,7 @@ BEGIN {
         do 'tools/check_mi_mods.pl';
     } else {
         unshift @INC, 'inc';
+        use lib '.';
         require inc::Module::Install;
         Module::Install->import;
     }
