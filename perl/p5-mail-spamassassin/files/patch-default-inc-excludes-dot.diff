--- spamc/configure.pl.orig	2017-07-03 07:14:46.000000000 -0700
+++ spamc/configure.pl	2017-07-03 07:15:13.000000000 -0700
@@ -66,7 +66,7 @@
   # Do the same thing as for the preprocessor below.
   package version_h;
   my $Z = $0;
-  local $0    = "version.h.pl";
+  local $0    = "./version.h.pl";
   local @ARGV = ();
   # Got to check for defined because the script returns shell error level!
   unless (defined do $0) {
