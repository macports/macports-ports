# $Id$

PortSystem      1.0
PortGroup       php5extension 1.0

php5extension.setup hidef 0.1.12 pecl
license         PHP
categories      php devel
platforms       darwin
maintainers     ryandesign openmaintainer
description     Constants for real

long_description \
    Allow definition of user defined constants in simple ini files, which are \
    then processed like internal constants, without any of the usual \
    performance penalties.

checksums       rmd160  ac39d8d9f1c4a4eb261f998ccf4ad36fe6f519bf \
                sha256  7604904459576c5cf23eb66142672793c3a86d3a83252710973d3fd8d7ceb9de

use_parallel_build yes

test.run yes

post-destroot {
    set docdir ${prefix}/share/doc/${name}
    xinstall -d ${destroot}${docdir}
    xinstall -m 644 -W ${worksrcpath} CREDITS ${destroot}${docdir}
}

platform darwin {
    patchfiles-append patch-hidef.c.diff
}
