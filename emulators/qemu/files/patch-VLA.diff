# https://github.com/macports/macports-ports/pull/14807#issuecomment-1184586924

--- tests/qtest/libqmp.c.orig	2023-08-11 06:51:48.000000000 +0800
+++ tests/qtest/libqmp.c	2023-08-18 22:22:52.000000000 +0800
@@ -101,7 +101,8 @@
 {
     ssize_t ret;
     struct msghdr msg = { 0 };
-    char control[CMSG_SPACE(sizeof(int) * SOCKET_MAX_FDS)] = { 0 };
+    char control[CMSG_SPACE(sizeof(int) * SOCKET_MAX_FDS)];
+    memset(control, 0, CMSG_SPACE(sizeof(int) * SOCKET_MAX_FDS));
     size_t fdsize = sizeof(int) * fds_num;
     struct cmsghdr *cmsg;
     struct iovec iov = { .iov_base = (char *)buf, .iov_len = buf_size };
