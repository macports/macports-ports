--- backends/sdl/build.rules	Sat Aug 24 12:41:32 2002
+++ backends/sdl/build.rules.new	Sat Nov 22 14:13:50 2003
@@ -13,5 +13,5 @@
 # LIBS     += -lGL
 
 # Uncomment this in addition to the above if you compile on Mac OS X
-# DEFINES += -DMACOSX
-# LIBS	+= -framework QuickTime -framework AudioUnit
+DEFINES += -DMACOSX
+LIBS	+= -framework QuickTime -framework AudioUnit
