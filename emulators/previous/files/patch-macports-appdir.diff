--- src/CMakeLists.txt.orig	2025-10-12 10:52:00.000000000 -0700
+++ src/CMakeLists.txt	2025-10-12 10:52:30.000000000 -0700
@@ -162,7 +162,11 @@
 
 
 if(ENABLE_OSX_BUNDLE)
-	install(TARGETS ${APP_NAME} BUNDLE DESTINATION /Applications)
+	if(NOT BUNDLE_INSTALL_DIR)
+		set(BUNDLE_INSTALL_DIR "/Applications")
+	endif(NOT BUNDLE_INSTALL_DIR)
+	message("  - Bundle install directory: ${BUNDLE_INSTALL_DIR}")
+	install(TARGETS ${APP_NAME} BUNDLE DESTINATION ${BUNDLE_INSTALL_DIR})
 else()
 	install(TARGETS ${APP_NAME} RUNTIME DESTINATION ${BINDIR})
 	install(FILES Previous-icon.bmp DESTINATION ${DATADIR})

