# $Id: Portfile,v 1.5 2004/12/28 11:31:47 mww Exp $

PortSystem 1.0
PortGroup python24 1.0

name			py-twisted
version			1.3.0
revision		2
categories		python devel net
maintainers		mww@opendarwin.org
description		An event-based Python framework for internet applications
long_description	Twisted is an event-driven networking framework \
				written in Python. Twisted supports TCP, UDP, SSL/TLS, \
				multicast, Unix sockets, a large number of protocols \
				(including HTTP, NNTP, SSH, IRC, FTP, and others), and \
				much more.

homepage		http://www.twistedmatrix.com/
master_sites	http://twistedmatrix.com/downloads/ \
				http://twisted.sourceforge.net/
distname		Twisted-${version}
use_bzip2		yes
checksums		md5 aaed1c9d006f9b72ea82ad978989fcdf

post-destroot   { 
	file delete -force ${destroot}${prefix}/share/doc/${name}
	xinstall -m 644 -W ${worksrcpath}/doc/man/ ckeygen.1 manhole.1 \
		tkconch.1 conch.1 mktap.1 tkmktap.1 im.1 t-im.1 trial.1 \
		tap2deb.1 twistd.1 lore.1 tap2rpm.1 websetroot.1 mailmail.1 \
		tapconvert.1 ${destroot}${prefix}/share/man/man1/
	file delete -force ${worksrcpath}/doc/man/
	file copy ${worksrcpath}/doc ${destroot}${prefix}/share/doc/${name}
}
