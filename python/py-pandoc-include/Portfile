# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4

PortSystem          1.0
PortGroup           python 1.1

name                py-pandoc-include
python.rootname     pandoc-include
version             1.4.4
revision            0

categories-append   print
supported_archs     noarch
platforms           {darwin any}
license             MIT
maintainers         nomaintainer

description         A Python module to handle multi-part .md files
long_description    Pandoc-include allows a markdown file to be split in multiple chunks \
                    which are assembled at runtime

homepage            https://pypi.org/project/pandoc-include

master_sites        https://github.com/DCsunset/pandoc-include/archive/refs/tags
distname            v${version}

checksums           rmd160  e27a82de7011d69b0c7594c44460dcd3d10d65b9 \
                    sha256  2266f8be32cd7e684d742e6fdc592bb406b238dd8d1174d201aeef77d17fe112 \
                    size    130603

worksrcdir          pandoc-include-1.4.4
patchfiles          patch-main_py.diff

python.versions     310 311 312 313 314 315 315t

if {${name} ne ${subport}} {
    build               {}
    depends_lib         port:py${python.suffix}-pip \
                        port:py${python.suffix}-setuptools
    depends_run         port:py${python.suffix}-panflute \
                        port:py${python.suffix}-natsort \
                        port:py${python.suffix}-lxml
    destroot.cmd        pip-${python.branch}
    destroot.args       install --ignore-installed --no-cache-dir --no-dependencies --root ${destroot} \
                        --no-build-isolation --no-index ${distpath}/${distfiles}
    destroot.post_args
}
