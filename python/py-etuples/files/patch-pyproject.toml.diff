--- pyproject.toml.orig	2025-07-22 11:13:25
+++ pyproject.toml	2025-07-22 11:13:35
@@ -1,5 +1,5 @@
 [build-system]
-requires = ["setuptools>=80.0.0", "setuptools-scm"]
+requires = ["setuptools", "setuptools-scm"]
 build-backend = "setuptools.build_meta"
 
 [project]
