# $Id: Portfile,v 1.1 2004/04/13 03:19:05 blb Exp $

PortSystem			1.0
name				py-cssutils
version				0.51
categories			python
maintainers			blb@pobox.com
description			Parse and build CSS Cascading Style Sheets
long_description	${description}

platforms			darwin

homepage			http://cthedot.de/cssutils/
master_sites		${homepage}
distname			cssutils-${version}
use_zip				yes

checksums			md5 4f2449508f32afb09732c57dfeb3cb44

depends_lib			bin:python:python23

use_configure		no

build.cmd			python setup.py
build.target		build

destroot.cmd		${build.cmd}
destroot.destdir	--prefix=${destroot}/${prefix}

post-destroot {
	xinstall -m 755 -d ${destroot}/${prefix}/share/doc/${name}
	file rename ${destroot}/${prefix}/LICENSE.txt \
		${destroot}/${prefix}/share/doc/${name}/LICENSE.txt
	xinstall -m 644 -W ${worksrcpath} README.txt \
		${destroot}/${prefix}/share/doc/${name}
}

