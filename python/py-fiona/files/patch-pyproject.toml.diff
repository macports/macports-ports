--- pyproject.toml.orig	2025-09-19 12:06:35
+++ pyproject.toml	2025-09-19 12:07:01
@@ -1,5 +1,5 @@
 [build-system]
-requires = ["setuptools>=67.8", "cython~=3.0.2"]
+requires = ["setuptools>=67.8", "cython>=3.0.2"]
 build-backend = "setuptools.build_meta"
 
 [project]
