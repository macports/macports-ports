# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4

PortSystem          1.0
PortGroup           python 1.1
PortGroup           select 1.0

name                py-hatchling
version             1.32.0
revision            0

license             MIT
maintainers         {mps @Schamschula} openmaintainer
description         This is the extensible, standards compliant build backend used by Hatch.
long_description    {*}${description}
homepage            https://hatch.pypa.io/latest/

supported_archs     noarch
platforms           {darwin any}

checksums           rmd160  0ac4d20c76e0059b8b5d98b960aae4beb82f50b1 \
                    sha256  0bdbde4a52b06c37e3eca395f85a762bf0ef06fe374fd8ae429dc6be10230f5f \
                    size    57783

python.versions     310 311 312 313 314 315 315t

if {${name} ne ${subport}} {
    # Bootstraps itself, no backend dependencies needed
    python.pep517_backend

    depends_lib-append  \
                    port:py${python.suffix}-packaging \
                    port:py${python.suffix}-pathspec \
                    port:py${python.suffix}-pluggy \
                    port:py${python.suffix}-tomlkit \
                    port:py${python.suffix}-trove-classifiers

    depends_run-append \
                    port:hatchling_select

    select.group    hatchling
    select.file     ${filespath}/py${python.suffix}-hatchling

    if {${python.version} < 311} {
        depends_lib-append  \
                    port:py${python.suffix}-tomli
    }
}
