diff --git pyproject.toml pyproject.toml
index 9649c106076f..68c93d908593 100644
--- pyproject.toml
+++ pyproject.toml
@@ -1,5 +1,5 @@
 [build-system]
-requires = ["setuptools ~= 67.8", "setuptools_scm[toml] ~= 7.1"]
+requires = ["setuptools", "setuptools_scm[toml]"]
 build-backend = "setuptools.build_meta"
 
 [project]
