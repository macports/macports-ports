# $Id: Portfile,v 1.1 2004/01/18 14:50:25 mww Exp $

PortSystem 1.0

name		py-vorbis
version		1.3
categories	python
platforms	darwin
maintainers	mww@opendarwin.org
description	Python module for Ogg Vorbis audio encoding and decoding
long_description	${description}

homepage	http://www.vorbis.com/
master_sites	http://www.vorbis.com/files/1.0.1/unix/py/
distname	pyvorbis-${version}
checksums	md5 506ebbf2ea3040857ff2af5d09125ef8

depends_lib	bin:python:python \
		lib:libvorbis:libvorbis \
		lib:XXX:py-ogg

configure.cmd	python config_unix.py
configure.pre_args	--prefix ${prefix}

build.cmd	python setup.py
build.target	build
build.env	CFLAGS=-logg

destroot.cmd	python setup.py
destroot.destdir	--prefix=${destroot}${prefix}

post-destroot	{
	file mkdir ${destroot}${prefix}/share/doc/${name}
	file copy ${worksrcpath}/AUTHORS ${worksrcpath}/COPYING \
		${worksrcpath}/ChangeLog ${worksrcpath}/README \
		${worksrcpath}/NEWS ${destroot}${prefix}/share/doc/${name}
}
