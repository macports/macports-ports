--- setup.py.orig	2026-01-07 12:47:53
+++ setup.py	2026-01-07 12:48:13
@@ -91,6 +91,9 @@
                 c_sources.append(source)
         extra_args = ext.extra_compile_args or []
 
+        cxxflags = os.environ['CXXFLAGS'].split()
+        extra_args.extend(cxxflags)
+
         objects = []
 
         macros = ext.define_macros[:]
@@ -189,7 +192,7 @@
 
 if USE_SYSTEM_BROTLI:
     import pkgconfig
-    
+
     REQUIRED_BROTLI_SYSTEM_LIBRARIES = ["libbrotlicommon", "libbrotlienc", "libbrotlidec"]
 
     define_macros = []
