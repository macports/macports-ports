--- pyobjc-core/Modules/objc/libffi_support.h
+++ #<buffer libffi_support.h>
@@ -1,7 +1,7 @@
 #ifndef PyObjC_FFI_SUPPORT_H
 #define PyObjC_FFI_SUPPORT_H
 
-#include <ffi/ffi.h>
+#include <ffi.h>
 
 #ifndef FFI_CLOSURES
 #error "Need FFI_CLOSURES!"

--- pyobjc-core/Modules/objc/libffi_support.m
+++ #<buffer libffi_support.m>
@@ -4480,7 +4480,7 @@
 
 #if PyObjC_BUILD_RELEASE >= 1015
     if (@available(macOS 10.15,*)) {
-        cl     = ffi_find_closure_for_code_np(closure);
+        cl     = ffi_find_closure_for_code(closure);
         retval = cl->user_data;
         PyObjCFFI_FreeCIF(cl->cif);
         ffi_closure_free(cl);
@@ -4495,7 +4495,7 @@
 #else
 #pragma clang diagnostic push
 #pragma clang diagnostic ignored "-Wunguarded-availability-new"
-    cl     = ffi_find_closure_for_code_np(closure);
+    cl     = ffi_find_closure_for_code(closure);
     retval = cl->user_data;
     PyObjCFFI_FreeCIF(cl->cif);
     ffi_closure_free(cl);
