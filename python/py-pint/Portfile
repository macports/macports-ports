# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4

PortSystem          1.0
PortGroup           python 1.1

name                py-pint
version             0.26.1
revision            0

categories-append   science
license             BSD
maintainers         {mps @Schamschula} openmaintainer
description         Pint: a Python units library
long_description    Pint is Python module/package to define, operate and \
                    manipulate physical quantities: the product of a numerical \
                    value and a unit of measurement.  It allows arithmetic \
                    operations between them and conversions from and to \
                    different units.
homepage            https://pint.readthedocs.org/

supported_archs     noarch
platforms           {darwin any}

checksums           rmd160  87a002ce8cb333ce183ebf95a9baff83bd5c080b \
                    sha256  1bbde36eae57a5a289cd05081c6405618a5899814940752064ce351cd0204f71 \
                    size    273631

python.versions     310 311 312 313 314 315 315t

python.pep517_backend \
                    hatch

if {${name} ne ${subport}} {
    depends_build-append \
                    port:py${python.suffix}-hatch-vcs

    depends_lib-append \
                    port:py${python.suffix}-flexcache \
                    port:py${python.suffix}-flexparser \
                    port:py${python.suffix}-platformdirs \
                    port:py${python.suffix}-typing_extensions

    depends_test-append \
                    port:py${python.suffix}-pytest \
                    port:py${python.suffix}-pytest-benchmark \
                    port:py${python.suffix}-pytest-cov \
                    port:py${python.suffix}-pytest-mpl \
                    port:py${python.suffix}-pytest-subtests

    variant babel description {build with babel support} {
        depends_lib-append \
                    port:py${python.suffix}-babel
    }

    variant matplotlib description {build with matplotlib support} {
        depends_lib-append \
                    port:py${python.suffix}-matplotlib
    }

    variant numpy description {build with numpy support} {
        depends_lib-append \
                    port:py${python.suffix}-numpy
    }

    if {${python.version} > 311} {
        variant optype description {build with optype support} {
            depends_lib-append \
                        port:py${python.suffix}-optype
        }
    }

    if {${python.version} > 310} {
        variant pint_pandas description {build with pint-pandas support, \
                                         has circular dependency on pint: \
                                         first install without this variant} {
            depends_lib-append \
                        port:py${python.suffix}-pint-pandas
        }
    }

    variant scipy description {build with scipy support} {
        depends_lib-append \
                    port:py${python.suffix}-scipy
    }

    variant uncertainties description {build with uncertainties support} {
        depends_lib-append \
                    port:py${python.suffix}-uncertainties
    }

    variant xarray description {build with xarray support} {
        depends_lib-append \
                    port:py${python.suffix}-xarray
    }

    default_variants \
                    +babel \
                    +matplotlib \
                    +numpy \
                    +scipy \
                    +uncertainties \
                    +xarray

    if {${python.version} > 311} {
        default_variants \
                    +optype
    }

    post-destroot {
        set docdir ${prefix}/share/doc/${subport}
        xinstall -d ${destroot}${docdir}
        xinstall -m 0644 -W ${worksrcpath} AUTHORS LICENSE \
           README.rst ${destroot}${docdir}
    }

    test.run        yes
}
