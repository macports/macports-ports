--- pyproject.toml.orig	2024-08-21 03:30:31
+++ pyproject.toml	2024-08-21 05:16:52
@@ -2,7 +2,7 @@
 # SPDX-License-Identifier: MPL-2.0
 
 [build-system]
-requires = ["setuptools >= 61, <= 72", "wheel >= 0.37", "pybind11 >= 2.10.1"]
+requires = ["setuptools >= 61", "wheel >= 0.37", "pybind11 >= 2.10.1"]
 build-backend = "setuptools.build_meta"
 
 [project]
