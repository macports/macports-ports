# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4

PortSystem          1.0
PortGroup           stub 1.0
PortGroup           select 1.0

name                copier_select
version             0.1
revision            0
categories          python
supported_archs     noarch
platforms           any

maintainers         {gunnar @gunnar} openmaintainer

description         Common files for selecting default Copier CLI
long_description    This port installs files so that the 'port select' command \
                    can create links to the preferred default Copier executable.

select.entries      {copier base} \
                    {copier none}

livecheck.type      none
