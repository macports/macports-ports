--- pyproject.toml.orig	2025-05-21 12:40:26
+++ pyproject.toml	2025-05-21 12:40:35
@@ -1,5 +1,5 @@
 [build-system]
-requires = ["setuptools", "setuptools-scm", "mypy>=1.4.1,<=1.15.0"]
+requires = ["setuptools", "setuptools-scm"]
 build-backend = "setuptools.build_meta"
 
 [project]
