--- pyproject.toml.orig	2025-02-12 23:15:32
+++ pyproject.toml	2025-02-12 23:15:42
@@ -1,5 +1,5 @@
 [build-system]
-requires = ["setuptools", "setuptools-scm", "mypy>=1.4.1,<=1.14.0"]
+requires = ["setuptools", "setuptools-scm"]
 build-backend = "setuptools.build_meta"
 
 [project]
