# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4

PortSystem          1.0
PortGroup           python 1.0

name                py-rstcheck
version             4.1.0
revision            0

categories-append   devel
supported_archs     noarch
license             MIT
maintainers         {reneeotten @reneeotten} openmaintainer

description         Checks syntax of reStructuredText and code blocks nested within it
long_description    ${description}

homepage            https://github.com/myint/rstcheck

checksums           rmd160  01ced28eca41f5fb6fe91ac33cfbcedc01732968 \
                    sha256  565efb68a8bbed417a1d7b87dc3f0869b8910f471d9829a9fcb7f009bfc6c219 \
                    size    22670

python.versions     37 38 39 310

if {${subport} ne ${name}} {
    depends_build-append \
                    port:py${python.version}-setuptools

    depends_lib-append \
                    port:py${python.version}-docutils

    if {${python.version} < 38} {
        depends_lib-append \
                    port:py${python.version}-importlib-metadata
    }

    post-destroot {
        set docdir ${prefix}/share/doc/${subport}
        xinstall -d ${destroot}${docdir}
        xinstall -m 0644 -W ${worksrcpath} LICENSE \
            AUTHORS.rst README.rst ${destroot}${docdir}
    }
}
