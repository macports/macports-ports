Upstream fix for poetry build backend.
See: https://github.com/nicfit/eyeD3/pull/589

diff --git a/pyproject.toml b/pyproject.toml
index 29eb619..672efa8 100644
--- pyproject.toml.orig
+++ pyproject.toml
@@ -1,6 +1,6 @@
 [build-system]
-requires = ["poetry>=0.12"]
-build-backend = "poetry.masonry.api"
+requires = ["poetry-core"]
+build-backend = "poetry.core.masonry.api"


 [tool.regarding]
