--- pyproject.toml.orig	2025-09-11 22:15:20
+++ pyproject.toml	2025-09-11 22:15:28
@@ -1,5 +1,5 @@
 [build-system]
-requires = ["Cython~=3.0.11", "setuptools>=77", "setuptools-scm>=8"]
+requires = ["Cython", "setuptools>=77", "setuptools-scm>=8"]
 build-backend = "setuptools.build_meta"
 
 [project]
