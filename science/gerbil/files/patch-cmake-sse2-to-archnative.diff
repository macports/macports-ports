--- cmake/EnableSSE2.cmake.orig	2021-05-11 17:40:45.000000000 -0400
+++ cmake/EnableSSE2.cmake	2021-05-11 17:40:01.000000000 -0400
@@ -14,6 +14,6 @@
 			add_definitions("/arch:SSE2")
 		endif()
 	else(MSVC) # assume gcc
-		add_definitions("-msse2")
+		add_definitions("-march=native")
 	endif(MSVC)
 endmacro()
