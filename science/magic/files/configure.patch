When python3 is found, use the python3 that was found rather than
assuming it is called "python3" and is in the PATH. This allows the user
to specify a different python3 by using the configure argument
ac_cv_path_PYTHON3=...
--- scripts/configure.orig	2022-03-31 01:00:12.000000000 -0500
+++ scripts/configure	2022-04-08 19:06:29.000000000 -0500
@@ -306,7 +306,7 @@
    fi
 
 else
-   MCPP="\${MAGICDIR}/scripts/preproc.py -ccomm"
+   MCPP="${PYTHON3} \${MAGICDIR}/scripts/preproc.py -ccomm"
    MSED="cat"
    usingPython3=1
 fi
