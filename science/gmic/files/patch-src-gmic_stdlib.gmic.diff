diff --git a/src/gmic_stdlib.gmic b/src/gmic_stdlib.gmic
index 5423ce0b..13da253a 100644
--- src/gmic_stdlib.gmic
+++ src/gmic_stdlib.gmic
@@ -28867,7 +28867,7 @@ RGBA : rgba
 #@cli shell_cols
 #@cli : Return the estimated number of columns of the current shell.
 shell_cols :
-  if ${-is_windows} u 80
+  if ${-is_windows}" || "${-is_macos} u 80
   else
     file_rand filename=${}
     l[] cols=80 x "tput cols > "$filename it $filename if isint({t}) cols={{t}} fi rm onfail cols=80 rm endl
