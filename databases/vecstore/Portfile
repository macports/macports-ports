# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4

PortSystem          1.0
PortGroup           github 1.0
PortGroup           cargo 1.0

github.setup        PhilipJohnBasile vecstore 1.0.0 v
github.tarball_from archive
revision            0

description         High-performance embeddable vector database

long_description    VecStore is a high-performance embeddable vector database \
                    with HNSW indexing, hybrid search capabilities, \
                    multi-language bindings, and comprehensive RAG toolkit. \
                    Features 100/100 feature matrix compared to competitors \
                    like Pinecone, Weaviate, and Qdrant.

categories          databases devel
license             MIT
maintainers         {github @PhilipJohnBasile} openmaintainer

checksums           rmd160  REPLACE_WITH_ACTUAL_RMD160 \
                    sha256  REPLACE_WITH_ACTUAL_SHA256 \
                    size    REPLACE_WITH_ACTUAL_SIZE

depends_build       port:pkgconfig \
                    port:protobuf3-cpp

build.args          --features server --bin vecstore-server

destroot {
    xinstall -m 0755 ${worksrcpath}/target/[cargo.rust_platform]/release/vecstore-server \
        ${destroot}${prefix}/bin/

    xinstall -d ${destroot}${prefix}/share/doc/${name}
    xinstall -m 0644 ${worksrcpath}/README.md ${destroot}${prefix}/share/doc/${name}
    xinstall -m 0644 ${worksrcpath}/LICENSE ${destroot}${prefix}/share/doc/${name}
}
