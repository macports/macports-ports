# $Id: Portfile,v 1.3 2004/12/04 15:49:25 mww Exp $

PortSystem 1.0

name			postgresql8
version			8.0.0rc1
categories		databases
platforms		darwin
maintainers		mww@opendarwin.org
description		release candidate of the most advanced open-source database available
long_description PostgreSQL is Object-Relational Database Management System. \
				It supports almost all SQL constructs, including transactions, \
				subselects, and user-defined types and functions. Write-ahead \
				logging means increased performance while retaining data \
				integrity. This is the release candidate of the upcoming \
				version 8.0.

homepage		http://www.postgresql.org/
master_sites	ftp://ftp.de.postgresql.org/mirror/postgresql/beta/
distname		postgresql-${version}
checksums		md5 cbc83baa2307a14443d4ee52a72c7d37
use_bzip2		yes

depends_lib		lib:readline:readline \
				path:${prefix}/bin/bison:bison

configure.args	--with-docdir=${prefix}/share/doc/${name} \
				--datadir=${prefix}/share/${name} \
				--sysconfdir=${prefix}/etc/pgsql8 \
				--mandir=${prefix}/share/man \
				--with-includes=${prefix}/include \
				--with-libraries=${prefix}/lib

build.type		gnu
build.target

test.run		yes
test.target		check
