--- src/common/fs_sys_helpers/macos.cpp.orig	2026-08-24 00:00:00
+++ src/common/fs_sys_helpers/macos.cpp	2026-09-02 00:00:00
@@ -76,7 +76,8 @@ get_current_exe_path([[maybe_unused]] std
 
 boost::filesystem::path
 get_package_data_folder() {
-  return get_current_exe_path({}) / "data";
+  auto bundle_data_folder = get_current_exe_path({}) / "data";
+  return boost::filesystem::is_directory(bundle_data_folder) ? bundle_data_folder : mtx::fs::to_path(MTX_PKG_DATA_DIR);
 }
 
 }
