--- src/SFML/Network/CMakeLists.txt.orig
+++ src/SFML/Network/CMakeLists.txt
@@ -56,7 +56,10 @@
     target_link_libraries(sfml-network PRIVATE resolv)
 endif()

-if(SFML_USE_SYSTEM_DEPS)
+# Always use bundled mbedtls/libssh2; SFML's bundled mbedtls enables
+# MBEDTLS_THREADING_ALT with its own threading implementation, which
+# the system mbedtls does not provide.
+if(FALSE)
     find_package(MbedTLS REQUIRED)
     find_package(Libssh2 REQUIRED)
 else()
