diff --git a/include/mp4v2/file.h b/include/mp4v2/file.h
index 00c02e6..568aae1 100644
--- include/mp4v2/file.h
+++ include/mp4v2/file.h
@@ -330,10 +330,14 @@ bool MP4Optimize(
  */
 typedef bool( *ShouldParseAtomCallback )( uint32_t );
 
+#define VARIADIC_MACRO(_1,_2,NAME,...) NAME
+#define MP4Read(...) VARIADIC_MACRO(__VA_ARGS__, MP4Read2, MP4Read1)(__VA_ARGS__)
+#define MP4Read1(fileName) MP4Read2(fileName, NULL)
+
 MP4V2_EXPORT
-MP4FileHandle MP4Read(
+MP4FileHandle MP4Read2(
     const char* fileName,
-    ShouldParseAtomCallback cb = nullptr );
+    ShouldParseAtomCallback cb );
 
 /** Read an existing mp4 file.
  *
