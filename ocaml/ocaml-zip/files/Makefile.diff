--- Makefile.orig
+++ Makefile
@@ -80,10 +80,10 @@
 install-findlib: install
 install:
 	cp META-zip META && \
-        ocamlfind install zip META $(TOINSTALL) && \
+        ocamlfind install $(OCAMLFIND_INSTFLAGS) zip META $(TOINSTALL) && \
         rm META
 	cp META-camlzip META && \
-        ocamlfind install camlzip META && \
+        ocamlfind install $(OCAMLFIND_INSTFLAGS) camlzip META && \
         rm META

 uninstall:
