#!/bin/sh
PREFIX=__PREFIX__
GROWL=${PREFIX}/bin/growlnotify

${PREFIX}/bin/port $*

if [ $? -eq 0 ]; then
	echo -e "success:\n\t${*}" | ${GROWL} DarwinPorts
else
	echo -e "error:\n\t${*}" | ${GROWL} DarwinPorts
fi
