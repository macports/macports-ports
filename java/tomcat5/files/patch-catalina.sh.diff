--- ../jakarta-tomcat-catalina/catalina/src/bin/catalina.sh.orig	Tue Oct  5 10:39:11 2004
+++ ../jakarta-tomcat-catalina/catalina/src/bin/catalina.sh	Thu Oct 28 13:31:15 2004
@@ -126,6 +126,11 @@
   JAVA_ENDORSED_DIRS=`cygpath --path --windows "$JAVA_ENDORSED_DIRS"`
 fi
 
+# Run conf_setup.sh to check and repair the conf directory
+if [ -x "$CATALINA_HOME/bin/conf_setup.sh" ]; then
+	CATALINA_BASE="$CATALINA_BASE" "$CATALINA_HOME/bin/conf_setup.sh"
+fi
+
 # ----- Execute The Requested Command -----------------------------------------
 
 echo "Using CATALINA_BASE:   $CATALINA_BASE"
