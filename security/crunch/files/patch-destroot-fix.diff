--- Makefile.orig	2014-05-03 13:04:11.000000000 -0400
+++ Makefile	2019-11-15 22:32:39.000000000 -0500
@@ -37,7 +37,7 @@
 DOCDIR	    = $(PREFIX)/share/doc/$(PACKAGE)
 MANDIR	    = $(PREFIX)/share/man/man1
 
-INSTALL	    = sudo $(shell which install)
+INSTALL	    = $(shell which install)
 CC	    = $(shell which gcc)
 LIBFLAGS    = -lm
 THREADFLAGS = -pthread
