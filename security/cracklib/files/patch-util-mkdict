--- ../cracklib,2.7.orig/util/mkdict	Wed Dec 31 01:37:14 1997
+++ util/mkdict	Wed Apr 21 09:47:30 2004
@@ -14,10 +14,10 @@
 SORT="sort"
 ###SORT="sort -T /tmp"
 
-cat $* |
+zcat -f $* |
 	tr '[A-Z]' '[a-z]' |
 	tr -cd '\012[a-z][0-9]' |
-	$SORT |
-	uniq |
 	grep -v '^#' |
-	grep -v '^$'
+	grep -v '^$' |
+	$SORT |
+	uniq
