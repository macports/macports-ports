--- src/build-data/botan-config.in	2009-01-20 07:25:17.000000000 +0100
+++ botan-config.in	2009-01-20 23:37:54.000000000 +0100
@@ -54,10 +54,10 @@
     --libs)
         if [ $prefix != "/usr" -a $prefix != "/usr/local" ]
         then
-           printf "-L$prefix/$libdir "
+           echo -L$prefix/$libdir @{var:link_to} -lbotan
+        else
+           echo @{var:link_to} -lbotan
         fi
-
-        echo @{var:link_to} -lbotan
         ;;
     *)
         usage
