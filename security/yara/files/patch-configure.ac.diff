--- configure.ac.orig	2026-07-28 09:04:21
+++ configure.ac	2026-07-29 06:52:15
@@ -50,8 +50,8 @@
     CFLAGS="$CFLAGS -DUSE_NO_PROC"
   fi],
   [case $host_os in
-    darwin*) CFLAGS="$CFLAGS -I/opt/local/include"
-             LDFLAGS="$LDFLAGS -L/opt/local/lib"
+    darwin*) CFLAGS="$CFLAGS -I%PREFIX%/include"
+             LDFLAGS="$LDFLAGS -L%PREFIX%/lib"
              if which brew >/dev/null 2>&1; then
                brew_prefix=$(brew --prefix)
                CFLAGS="$CFLAGS -I$brew_prefix/include"
