name: Auto Run Main Workflow

on:
  push:
    branches:
      - main

jobs:
  auto_run:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run Main Workflow
        uses: ./.github/workflows/main.yml
