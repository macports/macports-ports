--- gozilla/Makefile.in.orig	2014-11-28 17:39:40.000000000 -0800
+++ gozilla/Makefile.in	2014-11-28 17:40:11.000000000 -0800
@@ -353,7 +353,7 @@
 EXTRA_DIST = $(man_MANS) const.h manual.in
 
 # for osx-default
-gtagsdir = ${localstatedir}/gtags
+gtagsdir = ${DESTDIR}${localstatedir}/gtags
 all: all-am
 
 .SUFFIXES:
@@ -734,8 +734,8 @@
 	uninstall-man1
 
 install-data-hook:
-	mkdir -p ${gtagsdir}
-	chmod 777 ${gtagsdir}
+	mkdir -p ${DESTDIR}${gtagsdir}
+	chmod 777 ${DESTDIR}${gtagsdir}
 
 # Tell versions [3.59,3.63) of GNU make to not export all variables.
 # Otherwise a system limit (for SysV at least) may be exceeded.
