--- Makefile.in	Fri Apr 22 22:21:23 2005
+++ Makefile.in.new	Fri Apr 22 22:23:21 2005
@@ -331,6 +331,10 @@
 	$(libmpfr_a_AR) libmpfr.a $(libmpfr_a_OBJECTS) $(libmpfr_a_LIBADD)
 	$(RANLIB) libmpfr.a
 
+libmpfr.$(VERSION).dylib: $(libmpfr_a_OBJECTS)
+	$(CC) -dynamiclib $(libmpfr_a_OBJECTS) -o $@ -install_name $(libdir)/$@ -current_version $(VERSION) $(LDFLAGS) -lgmp
+
+
 mostlyclean-compile:
 	-rm -f *.$(OBJEXT)
 
