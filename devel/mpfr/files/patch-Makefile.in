--- Makefile.in	2005-09-19 15:32:02.000000000 +0200
+++ Makefile.in	2005-09-29 16:06:07.000000000 +0200
@@ -392,6 +392,10 @@
 	@rm -f tuneup$(EXEEXT)
 	$(LINK) $(tuneup_LDFLAGS) $(tuneup_OBJECTS) $(tuneup_LDADD) $(LIBS)
 
+libmpfr.$(VERSION).dylib: $(libmpfr_a_OBJECTS)
+	$(CC) -dynamiclib $(libmpfr_a_OBJECTS) -o $@ -install_name $(libdir)/$@ -current_version $(VERSION) $(LDFLAGS) -lgmp
+
+
 mostlyclean-compile:
 	-rm -f *.$(OBJEXT)
 
