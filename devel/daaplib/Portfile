# $Id: Portfile,v 1.2 2003/11/17 21:19:55 jkh Exp $

PortSystem 1.0
name		daaplib
version		0.1
categories	devel
maintainers	mww@opendarwin.org
description	small library for reading and writingg DAAP streams
long_description	daaplib is a very tiny and portable C++ library which \
			helps to read and write low-level DAAP streams in memory. the code is \
			straightforward and can mirror the structure of the data fairly well.

homepage	http://www.deleet.de/projekte/daap/daaplib/
master_sites	${homepage}
use_zip		yes
extract.suffix	.zip
distfiles	${name}.${version}${extract.suffix}
checksums	md5 dd3d7b09dd72f9a50552a1f7f8719c79

configure.dir	${workpath}/daaplib.${version}/daaplib/src
configure	{ reinplace "s|/usr/local|${destroot}${prefix}|g" \
		    ${configure.dir}/makefile }

build.dir	${configure.dir}
build.target
destroot.dir	${build.dir}
