--- MaciASL/AppDelegate.m
+++ MaciASL/AppDelegate.m
@@ -404,7 +404,8 @@
         return;
     }
     
-    rect.size.width -= 2;
+    rect.origin.x = 0;
+    rect.size.width = MAX(0, NSWidth(self.bounds) - 2);
     rect.origin.y -= (NSInteger)(scrollView.documentVisibleRect.origin.y + rect.origin.y) % height + 1;
     rect.size.height = height;
     while (start < stop) {
