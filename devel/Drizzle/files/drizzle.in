#!/bin/bash

CONFIGDIR="${HOME}/.config/drizzle"
mkdir -p "${CONFIGDIR}"
cd "${CONFIGDIR}"

exec java -Xmx800m \
-DDrizzle.IsLauncher=false \
-Dcom.apple.mrj.application.apple.menu.about.name=Drizzle \
-Xdock:name=Drizzle \
-jar "@PREFIX@/share/java/drizzle.jar" "$@"
