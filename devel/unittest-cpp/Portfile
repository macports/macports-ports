# $Id$

PortSystem 1.0

name                    unittest-cpp
version                 1.3
categories              devel
platforms               darwin
maintainers             uni-koblenz.de:guidolorenz
description             lightweight unit testing framework for C++
long_description        UnitTest++ is a lightweight unit testing framework for C++. \
                        It was designed to do test-driven development on a wide variety \
                        of platforms. Simplicity, portability, speed, and small footprint \
                        are all very important aspects of UnitTest++.\

homepage                http://unittest-cpp.sourceforge.net/

master_sites            sourceforge
checksums               md5 36c226068c3c7aa79aef5956db2adab8 \
                        sha1 1b9e7e496ce71349a7022e7c40cb07556acdf6a6 \
                        rmd160 a9498a0aeba10dabece6c7e77a0d6a6ac5eddfd7

use_zip                 yes
worksrcdir              UnitTest++

use_configure           no

build.target            libUnitTest++.a

test.run                yes
test.target             TestUnitTest++

destroot {
    xinstall -m 755 -d ${destroot}${prefix}/include/${name}/Posix
    eval xinstall -m 640 [glob ${worksrcpath}/src/*.h] \
        ${destroot}${prefix}/include/${name}
    eval xinstall -m 640 [glob ${worksrcpath}/src/Posix/*.h] \
        ${destroot}${prefix}/include/${name}/Posix
    xinstall -m 640 ${worksrcpath}/libUnitTest++.a \
        ${destroot}${prefix}/lib
}

universal_variant       no
