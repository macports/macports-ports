--- Makefile.in	Sat Oct 26 11:05:51 2002
+++ Makefile.in.new	Thu Sep  2 13:11:44 2004
@@ -30,7 +30,7 @@
 	$(MKDIR) $(libdir)
 	$(MKDIR) $(includedir)
 	$(INSTALL_DATA) $(TARGETLIB) $(libdir)/$(TARGETLIB)
-	for i in $(TARGET_INCS);do \
+	for i in $(TARGETINCS);do \
 	(set -x;$(INSTALL_DATA) $$i $(includedir)); \
 	done
 
