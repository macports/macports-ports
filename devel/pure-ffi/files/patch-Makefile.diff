--- Makefile.orig	2009-03-10 15:29:58.000000000 -0500
+++ Makefile	2009-03-15 20:43:13.000000000 -0500
@@ -77,8 +77,8 @@
 	rm -f *.aux *.log *.out
 
 install:
-	test -d "$(DESTDIR)$(libdir)/pure" || mkdir -p "$(DESTDIR)$(libdir)/pure"
-	cp ffi.pure ffi$(DLL) pure-ffi.html "$(DESTDIR)$(libdir)/pure"
+	test -d "$(DESTDIR)`readlink $(libdir)/pure`" || mkdir -p "$(DESTDIR)`readlink $(libdir)/pure`"
+	cp ffi.pure ffi$(DLL) pure-ffi.html "$(DESTDIR)`readlink $(libdir)/pure`"
 
 uninstall:
 	rm -f "$(DESTDIR)$(libdir)/pure/ffi.pure" "$(DESTDIR)$(libdir)/pure/ffi$(DLL)" "$(DESTDIR)$(libdir)/pure/pure-ffi.html"
