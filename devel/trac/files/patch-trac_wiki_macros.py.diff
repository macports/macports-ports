--- trac/wiki/macros.py
+++ trac/wiki/macros.py 
@@ -447,4 +447,7 @@
                     found.append(name)
                     yield name
+                except GeneratorExit:
+                    # never catch GeneratorExit
+                    raise
                 except Exception, e:
                     self.log.error('Failed to load wiki macro %s (%s)',
