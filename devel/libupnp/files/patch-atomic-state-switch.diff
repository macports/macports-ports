--- upnp/src/genlib/miniserver/miniserver.c.orig
+++ upnp/src/genlib/miniserver/miniserver.c
@@ -1636,7 +1636,7 @@ int StartMiniServer(
 
 	memset(&job, 0, sizeof(job));
 
-	switch (gMServState) {
+	switch ((MiniServerState)gMServState) {
 	case MSERV_IDLE:
 		break;
 	default:
@@ -1737,7 +1737,7 @@ int StopMiniServer(void)
 	char buf[256] = "ShutDown";
 	size_t bufLen = strlen(buf);
 
-	switch (gMServState) {
+	switch ((MiniServerState)gMServState) {
 	case MSERV_RUNNING:
 		gMServState = MSERV_STOPPING;
 		break;
