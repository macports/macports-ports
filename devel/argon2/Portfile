# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4

PortSystem          1.0
PortGroup           github 1.0

github.setup        P-H-C phc-winner-argon2 20161029
name                argon2
categories          devel
platforms           darwin
maintainers         {khindenburg @kurthindenburg} openmaintainer
license             Apache-2

description         Password-hashing implmention that won the P-H-C

long_description \
    Argon2 is a password-hashing function that summarizes the state of the \
    art in the design of memory-hard functions and can be used to hash \
    passwords for credential storage, key derivation, or other applications.

checksums           rmd160  35cbb2195d2ed9c53798c5c0eeeed05206d2b65e \
                    sha256  8e483c46f4f43ac6431e2accf84a0867453320f702beda34a019bdb3c74b5159

use_configure no

variant universal {}

build.env-append    CC=${configure.cc} \
                    CFLAGS='${configure.cflags} [get_canonical_archflags cc]' \
                    LDFLAGS='${configure.ldflags} [get_canonical_archflags ld]'

destroot.destdir    PREFIX=${destroot}${prefix}
destroot.env-append CC=${configure.cc} \
                    CFLAGS='${configure.cflags} [get_canonical_archflags cc]' \
                    LDFLAGS='${configure.ldflags} [get_canonical_archflags ld]'


test.run            yes
test.env-append     CC=${configure.cc} \
                    CFLAGS='${configure.cflags} [get_canonical_archflags cc]' \
                    LDFLAGS='${configure.ldflags} [get_canonical_archflags ld]'

