Homebrew-core does the same inreplace: stable rustc cannot -Zbuild-std.
--- scripts/build/deps/lolhtml.ts.orig
+++ scripts/build/deps/lolhtml.ts
@@ -81,7 +81,8 @@
     // -Cpanic=abort path.
     const canBuildStdImmediateAbort =
       cfg.darwin || cfg.freebsd || (cfg.linux && cfg.abi !== "musl" && cfg.abi !== "android");
-    if (cfg.release && canBuildStdImmediateAbort) {
+    // MacPorts/Homebrew: stable rustc cannot -Zbuild-std.
+    if (false) {
       spec.buildStd = true;
       spec.rustflags = [
         "-Zunstable-options",
