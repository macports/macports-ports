# -*- coding: utf-8; mode: tcl; tab-width: 4; indent-tabs-mode: nil; c-basic-offset: 4 -*- vim:fenc=utf-8:ft=tcl:et:sw=4:ts=4:sts=4

PortSystem          1.0
PortGroup           golang 1.0

go.setup            github.com/tmrts/boilr 0.3.0
revision            0

description         boilerplate template manager that generates files or \
                    directories from template repositories

long_description    Are you doing the same steps over and over again every \
                    time you start a new programming project?  Boilr is here \
                    to help you create projects from boilerplate templates. \
                    boilr can generate files or directories from templates \
                    and template repositories.

categories          devel
license             Apache-2
installs_libs       no

checksums           rmd160  b83d95eb27286fd1220a13995fb15948cf92bb1c \
                    sha256  6eb413b65952a2af13ee2d77d99b615c323463f1d78ea87df0759273a249eaf3 \
                    size    1096282

maintainers         {gmail.com:herby.gillot @herbygillot} \
                    openmaintainer

patch {
    # Rename go module whose original domain no longer exists
    reinplace {s|srcd.works/go-git.v4|gopkg.in/src-d/go-git.v4|} \
        ${worksrcpath}/pkg/util/git/git.go
}

# This project does not use Go modules, and as such go2port cannot generate
# a go.vendors block for it. So allow fetching dependencies at build time.
build.env-delete    GO111MODULE=off GOPROXY=off

build.cmd           go get
build.target        .

destroot {
    xinstall -m 755 ${gopath}/bin/${name} ${destroot}${prefix}/bin/
}
