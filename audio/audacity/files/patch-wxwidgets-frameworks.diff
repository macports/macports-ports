--- muse_deps/recipes/wxwidgets/meta.cmake.orig
+++ muse_deps/recipes/wxwidgets/meta.cmake
@@ -34,6 +34,9 @@
         message(FATAL_ERROR "[wxwidgets] ${wxconfig} --libs base failed: ${wx_err}")
     endif()
     separate_arguments(wx_cxx_list NATIVE_COMMAND "${wx_cxx}")
+    # Keep each framework and its name together when CMake processes link items.
+    string(REGEX REPLACE "-framework[ \t]+([^ \t]+)" "-Wl,-framework,\\1"
+           wx_libs "${wx_libs}")
     separate_arguments(wx_libs_list NATIVE_COMMAND "${wx_libs}")
 
     set(incs "")
