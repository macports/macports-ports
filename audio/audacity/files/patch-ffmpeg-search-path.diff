--- au3/modules/import-export/mod-ffmpeg/lib-ffmpeg-support/FFmpegFunctions.cpp.orig
+++ au3/modules/import-export/mod-ffmpeg/lib-ffmpeg-support/FFmpegFunctions.cpp
@@ -342,12 +342,9 @@
     }
 
 #elif defined(__WXMAC__)
+    paths.emplace_back(wxString::FromUTF8(INSTALL_PREFIX "/lib"));
     paths.emplace_back(wxT("/Library/Application Support/audacity/libs"));
     paths.emplace_back(wxT("/usr/local/lib/audacity"));
-    // x86_64 Homebrew
-    paths.emplace_back(wxT("/usr/local/lib"));
-    // ARM64 Homebrew
-    paths.emplace_back(wxT("/opt/homebrew/lib"));
 #endif
 
     return paths;
