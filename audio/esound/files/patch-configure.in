--- configure.in.orig	Wed Mar 23 00:31:46 2005
+++ configure.in	Wed Mar 23 00:35:36 2005
@@ -251,7 +251,8 @@
    if test "${ac_cv_header_CoreAudio_CoreAudio_h}" = "yes"; then
       found_sound=yes
       AC_DEFINE(DRIVER_COREAUDIO)
-      SOUND_LIBS="-Wl,-framework -Wl,CoreAudio"
+      SOUND_LIBS="-framework CoreAudio"
+      LIBS="$LIBS $SOUND_LIBS"
    fi
 
    if test "$found_sound" = "no"; then
