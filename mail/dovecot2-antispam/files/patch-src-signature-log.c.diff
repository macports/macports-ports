diff --git a/src/signature-log.c b/src/signature-log.c
--- a/src/signature-log.c
+++ b/src/signature-log.c
@@ -118,8 +118,11 @@
     if (sltc == NULL)
 	return NULL;
 
-    if (dict_init(cfg->dict_uri, DICT_DATA_TYPE_STRING, cfg->dict_user,
-		cfg->base_dir, &sltc->dict, NULL))
+    sltc->dict =
+	    dict_init(cfg->dict_uri, DICT_DATA_TYPE_STRING, cfg->dict_user,
+	    cfg->base_dir);
+
+    if (sltc->dict == NULL)
     {
 	i_free(sltc);
 	return NULL;
