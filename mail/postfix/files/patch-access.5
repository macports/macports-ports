--- man/man5/access.5	Sat Jan 25 23:48:24 2003
+++ man/man5/access.5.new	Sat Jun 21 13:36:53 2003
@@ -8,11 +8,11 @@
 .SH SYNOPSIS
 .na
 .nf
-\fBpostmap /etc/postfix/access\fR
+\fBpostmap __PREFIX/etc/postfix/access\fR
 
-\fBpostmap -q "\fIstring\fB" /etc/postfix/access\fR
+\fBpostmap -q "\fIstring\fB" __PREFIX/etc/postfix/access\fR
 
-\fBpostmap -q - /etc/postfix/access <\fIinputfile\fR
+\fBpostmap -q - __PREFIX/etc/postfix/access <\fIinputfile\fR
 .SH DESCRIPTION
 .ad
 .fi
@@ -25,7 +25,7 @@
 that serves as input to the \fBpostmap\fR(1) command.
 The result, an indexed file in \fBdbm\fR or \fBdb\fR format,
 is used for fast searching by the mail system. Execute the command
-\fBpostmap /etc/postfix/access\fR in order to rebuild the indexed
+\fBpostmap __PREFIX/etc/postfix/access\fR in order to rebuild the indexed
 file after changing the access table.
 
 When the table is provided via other means such as NIS, LDAP
