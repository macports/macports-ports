--- man/man5/transport.5	Fri Feb 28 19:28:36 2003
+++ man/man5/transport.5.new	Sat Jun 21 13:36:11 2003
@@ -8,11 +8,11 @@
 .SH SYNOPSIS
 .na
 .nf
-\fBpostmap /etc/postfix/transport\fR
+\fBpostmap __PREFIX/etc/postfix/transport\fR
 
-\fBpostmap -q "\fIstring\fB" /etc/postfix/transport\fR
+\fBpostmap -q "\fIstring\fB" __PREFIX/etc/postfix/transport\fR
 
-\fBpostmap -q - /etc/postfix/transport <\fIinputfile\fR
+\fBpostmap -q - __PREFIX/etc/postfix/transport <\fIinputfile\fR
 .SH DESCRIPTION
 .ad
 .fi
@@ -24,7 +24,7 @@
 that serves as input to the \fBpostmap\fR(1) command.
 The result, an indexed file in \fBdbm\fR or \fBdb\fR format, is used
 for fast searching by the mail system. Execute the command
-\fBpostmap /etc/postfix/transport\fR in order to rebuild the indexed
+\fBpostmap __PREFIX/etc/postfix/transport\fR in order to rebuild the indexed
 file after changing the transport table.
 
 When the table is provided via other means such as NIS, LDAP
