https://trac.macports.org/ticket/64635
--- qpdf/test_driver.cc.orig	2022-02-09 21:47:58.000000000 +1100
+++ qpdf/test_driver.cc	2022-02-10 22:14:02.000000000 +1100
@@ -22,6 +22,7 @@
 #include <qpdf/QIntC.hh>
 #include <qpdf/QPDFJob.hh>
 #include <qpdf/QPDFUsage.hh>
+#include <cmath>
 #include <iostream>
 #include <sstream>
 #include <algorithm>
@@ -3293,11 +3294,11 @@ static void test_85(QPDF& pdf, char cons
     assert(s == "42.0");
     double num = 0.0;
     assert(oh_i.getValueAsNumber(num));
-    assert(abs(num - 1.0) < 1e-100);
+    assert(std::abs(num - 1.0) < 1e-100);
     assert(oh_r.getValueAsNumber(num));
-    assert(abs(num - 42.0) < 1e-100);
+    assert(std::abs(num - 42.0) < 1e-100);
     assert(! oh_b.getValueAsNumber(num));
-    assert(abs(num - 42.0) < 1e-100);
+    assert(std::abs(num - 42.0) < 1e-100);
     s = "";
     assert(oh_n.getValueAsName(s));
     assert(s == "/Test") ;
