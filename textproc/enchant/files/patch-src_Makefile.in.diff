--- src/Makefile.in~	2007-05-24 06:42:48.000000000 -0400
+++ src/Makefile.in	2007-05-24 06:44:44.000000000 -0400
@@ -266,7 +266,7 @@
 	cd $(top_builddir) && $(MAKE) $(AM_MAKEFLAGS) am--refresh
 install-libLTLIBRARIES: $(lib_LTLIBRARIES)
 	@$(NORMAL_INSTALL)
-	test -z "$(libdir)" || $(MKDIR_P) "$(DESTDIR)$(libdir)"
+	test -z "$(DESTDIR)$(libdir)" || $(MKDIR_P) "$(DESTDIR)$(libdir)"
 	@list='$(lib_LTLIBRARIES)'; for p in $$list; do \
 	  if test -f $$p; then \
 	    f=$(am__strip_dir) \
@@ -332,7 +332,7 @@
 	-rm -rf .libs _libs
 install-libenchant_includeHEADERS: $(libenchant_include_HEADERS)
 	@$(NORMAL_INSTALL)
-	test -z "$(libenchant_includedir)" || $(MKDIR_P) "$(DESTDIR)$(libenchant_includedir)"
+	test -z "$(DESTDIR)$(libenchant_includedir)" || $(MKDIR_P) "$(DESTDIR)$(libenchant_includedir)"
 	@list='$(libenchant_include_HEADERS)'; for p in $$list; do \
 	  if test -f "$$p"; then d=; else d="$(srcdir)/"; fi; \
 	  f=$(am__strip_dir) \
