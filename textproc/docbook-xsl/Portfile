PortSystem 1.0

name 		docbook-xsl
version		1.55.0	
categories	textproc
description	the docbook xsl stylesheets
platforms	darwin


master_sites	http://easynews.dl.sourceforge.net/sourceforge/docbook/
checksums	md5 a42ff7e9ba3031d1ea65d160757cd166

configure 	{}
build		{}
install		{ system "mkdir -p ${prefix}/share/xsl/${name}/${version}"
		  system "cd ${worksrcpath} && \
			tar -cf - VERSION common fo html lib params xhtml | \
			tar -xf - -C ${prefix}/share/xsl/${name}/${version}"
		}

include contents
