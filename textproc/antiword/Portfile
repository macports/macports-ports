PortSystem 1.0
name		antiword
version		0.33
categories	textproc
maintainers	mij@soupnazi.org
description	Utility to read Microsoft Word (.doc) files
platforms	darwin

long_description Antiword is a utility that can read binary files from \
		 Microsoft Word 6, 7, 97, and 2000.

master_sites	http://www.winfield.demon.nl/linux/

checksums	md5 e2178402066bbb1f5b3a49d47316a1fc

#post-extract	{ reinplace "s|/usr/local|${prefix}|g" \
#		    ${workpath}/${worksrcdir}/Makefile.Linux
#		  reinplace "s|/usr/share|${prefix}/share|g" \
#		    ${workpath}/${worksrcdir}/Makefile.Linux }

configure	{}
post-configure	{ reinplace "s|\/usr\/local|${prefix}|g" \
		    ${workdir}/${worksrcdir}/Makefile.Linux
		  reinplace "s|\/usr|${prefix}|g" \
		    ${workdir}/${worksrcdir}/Makefile.Linux
		  reinplace "s|\/usr|${prefix}|g" \
		    ${workdir}/${worksrcdir}/antiword.h
		  reinplace "s|-pedantic||g" \
		    ${workdir}/${worksrcdir}/Makefile.Linux }

build.target.install global_install

post-install	{ system "cp -f ${workdir}/${worksrcdir}/Docs/antiword.1 \
		    ${prefix}/man/man1" }

include		contents
